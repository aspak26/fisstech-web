# PROGRESS.md — Fişştech Web Sitesi Geliştirme Takibi

> Bu dosya projenin neresinde olduğumuzu gösterir. Her önemli aşama tamamlandığında güncellenmeli. En son güncelleme tarihini üstte tut.

**Son güncelleme:** 21 Temmuz 2026 (pazarlama sitesi + marka rebrand)

---

## Genel Durum

✅ **Faz 1 tamamlandı ve gerçek kullanıcı hesabıyla canlı doğrulandı.**
✅ **Faz 2 (tüm bireysel kullanıcı modülleri) tamamlandı ve gerçek kullanıcı hesabıyla canlı doğrulandı** (3 canlı-test hatası bulunup düzeltildi — bkz. Kararlar Günlüğü). AI Sohbet kodu düzeldi ama Google tarafındaki API kotası nedeniyle kullanıcı kararıyla ayrı ele alınacak.
✅ **Faz 3 — Esnaf Modu TAMAMLANDI: ortak altyapı + 6 sektörün 6'sı da** (Yeme-İçme, Hızlı Perakende, Hizmet & Bakım, Toptancı & İmalatçı, Serbest Meslek & Proje, Yüksek Hacimli Satış), build/lint/type-check temiz — hiçbiri gerçek kullanıcı hesabıyla tarayıcıda uçtan uca teyit edilmedi, sonraki adım bu.
✅ **Faz 4 — gerçek zamanlı senkronizasyon (Supabase Realtime) tamamlandı VE AKTİF.** Kod tarafı build/lint/type-check temiz; `docs/sql/047_web_realtime.sql` kullanıcı tarafından Supabase Dashboard'da başarıyla çalıştırıldı (~40 tablo `supabase_realtime` publication'ına eklendi) — özellik artık gerçekten canlı, sadece kod olarak değil. Gerçek kullanıcı hesabıyla (iki sekme açıp bir yerde değişiklik yapıp diğerinin otomatik yenilendiğini görme) uçtan uca teyidi henüz yapılmadı.
✅ **21 Temmuz 2026 — kullanıcı gerçek hesabıyla canlı testte 1 hata + 4 istek bildirdi, hepsi ele alındı:** Harcamalarım gün başlıklarına tarih eklendi, Yatırımlarım canlı fiyat bağlantısı kuruldu, Gruplarım'a grafikli analiz sekmesi eklendi (**mobilde de olmayan, kullanıcı isteğiyle web'e özel yeni bir özellik**), Ayarlar'a Çıkış/Veri Sıfırlama/Hesap Silme eklendi, Dashboard'daki "Grup Bütçesi" kutusunun yanlış/terk edilmiş `family_memberships` tablosundan okuduğu (gerçek üyeliği asla göremeyen bir Faz-1 kalıntısı) bug'ı bulunup `group_members`'a düzeltildi. **Hâlâ açık (kod dışı kararlar bekliyor):** Premium/Esnaf abonelik yükseltme (ödeme altyapısı kararı), "Bir İşyerine Katıl" (ayrı bir workspace/çoklu-personel alt sistemi, kapsam dışı bırakıldı) — bkz. Açık Sorular.
✅ **21 Temmuz 2026 — Faz 6: Pazarlama Sitesi (Landing Page) + site geneli marka rebrand tamamlandı.** `/` artık herkese açık, gerçek bir tanıtım sayfası (önceden sadece `/dashboard`'a yönlendiren boş bir rota idi). Kullanıcı kararıyla site geneli renk kimliği mobildeki "Gül" temasından "Yeşil" temasına (koyu orman yeşili + yumuşak mint) geçirildi — SADECE landing page değil, dashboard dahil TÜM mevcut sayfalar (`styles/tokens.css` tek noktadan). Detaylar Kararlar Günlüğü'nde.
✅ **21 Temmuz 2026 — Faz 6 devamı: landing page 6 yeni/yeniden tasarlanmış bölümle "premium SaaS" seviyesine taşındı** (framer-motion eklendi): interaktif Step Slider ("Nasıl Çalışır?"), "Neden Fişştech?", "İş Akışı/Analiz" yüzen kartlar, iki kolonlu "Merak Edilenler" (SSS), Kapanış CTA, kapsamlı çok kolonlu Footer + 6 yeni yasal/destek stub rotası. Canlı tarayıcı testinde gerçek bir hata bulunup düzeltildi (mobilde yatay taşma — dönen yörünge halkasının transform bounding box'ı). Detaylar Kararlar Günlüğü'nde.

---

## Faz 0 — Planlama & Blueprint

- [x] Mobil uygulama ekranları incelendi, tasarım sistemi çıkarıldı
- [x] `Fisstech_Web_Blueprint.md` oluşturuldu
- [x] Tema stratejisi netleştirildi (Web: Açık/Koyu Mod)
- [x] `AGENTS.md` oluşturuldu
- [x] `PROGRESS.md` oluşturuldu
- [x] Kesin tasarım token'ları (hex kodları, font seçimi) netleştirildi
- [x] Teknoloji yığını (stack) kararı verildi

---

## Faz 1 — Tasarım Sistemi Kurulumu

- [x] Renk paleti kesinleştirildi (Açık Mod + Koyu Mod, tam hex değerleriyle, WCAG AA kontrastı hesaplanarak doğrulandı) — `styles/tokens.css`
- [x] Tipografi çifti seçildi (Bricolage Grotesque + Plus Jakarta Sans, `next/font/google` ile self-host)
- [x] Spacing/grid sistemi tanımlandı (8pt)
- [x] Temel UI komponentleri (buton, kart, input, badge, tab, skeleton, empty-state) oluşturuldu — `src/components/ui/`
- [x] Açık/Koyu mod geçiş mekanizması çalışır durumda (`next-themes`, class stratejisi)

---

## Faz 2 — Bireysel Kullanıcı Modülleri

- [x] Dashboard (Net Bakiye & Gelir-Gider özeti, ay seçici, hero kart, hızlı erişim, notlar, sabit giderler, son harcamalar)
- [x] Fiş Tarama (web'e özel: drag-drop / dosya seçici, tekli + uzun fiş çoklu mod, gerçek `scan-receipt` edge function entegrasyonu)
- [x] Harcamalarım — liste + manuel ekleme/düzenleme (kalemli, kategori seçimli), silme. `expenses`+`expense_items` gerçek tablolara yazıyor. **Mobil detay-parite geçişiyle genişletildi:** Dönem Seçin filtresi (Bu Hafta/Bu Ay/Geçen Ay/Son 3-6 Ay/Bu Yıl/Son 1 Yıl/Tüm Zamanlar + son 12 ay + özel aralık), tarihe göre gruplama ("Bugün"/"Dün"/gün adı/tarih), taksitli harcama (taksit sayısı, sabit aylık ödeme veya "her taksit farklı tutar" özel modu, `installment_plans` tablosu), "Kart (isteğe bağlı)" alanı (sadece kredi/banka kartı ödeme yönteminde), ürün adına göre otomatik kategori tahmini (`category_predictor.dart`'taki ~250 kurallık Türkçe sözlük birebir portlandı), "Gruba Ekle" (harcamayı bir veya birden çok gruba paylaşma + "ikisinde de/sadece grupta" görünürlük seçimi, `expense_groups` + `expenses.visibility`), "💳 Taksitler" filtre çipi.
- [x] Gelirlerim + Sabit Giderler — sekmeli tek sayfa, gerçek `incomes`/`fixed_expenses` + kategori tabloları, sabit gider "ödendi" toggle'ı. **Mobil detay-parite geçişiyle genişletildi:** üçüncü sekme olarak Değişken Giderler eklendi (aynı dönemdeki `expenses` kayıtları), ortak Dönem Seçin filtresi üç sekmeye de uygulanıyor, NET BAKİYE kartı (Gelir − Sabit − Değişken; mobildeki ekran kartı sadece Gelir − Sabit hesaplıyor, Excel raporu ise üçünü de düşüyor — bu tutarsızlık web'de bilinçli olarak DOĞRU/3-bileşenli formülle çözüldü), "Excel'e Aktar" butonu (`xlsx` paketiyle 5 sayfalı gerçek dosya indirme: Özet/Gelirler/Sabit Giderler/Değişken Giderler/Satın Alınan Ürünler — mobildeki native paylaşım sheet'i yerine tarayıcı indirmesi).
- [x] Net Bakiye & Gelir-Gider — bu ay özeti + son 6 ay gelir/gider/net trend grafiği (recharts).
- [x] Borçlarım — borç aldım/verdim ayrımı, toplam borç/alacak kartları, vade, "kapandı" toggle'ı. `user_debts` (text PK — `crypto.randomUUID()` ile üretiliyor). **Mobil detay-parite geçişiyle genişletildi:** üst kartlarda yön okları (Alacaklarım = yeşil ↑, Borçlarım = kırmızı ↓), filtre sekmeleri (Tümü/Alınan/Verilen/Ödenmedi — mobildeki gibi bu sekmeler her zaman sadece AÇIK kayıtları gösteriyor), ayrı bir "Borç Geçmişi" arşiv görünümü (sadece kapanmış kayıtlar, kendi dönem filtresiyle: Tümü/Bu Hafta/Bu Ay/Son 3-6 Ay/Bu Yıl/Geçen Yıl), kart üzerinde tip rozeti + renkli sol çerçeve + Türkçe ondalık ayraç destekli tutar girişi (`1.234,56` → `1234.56`), Hatırlatıcı toggle'ı (vade tarihi + panoda "Bugün/Yarın/Dün" vurgusu — **tarayıcı push bildirimi henüz YOK**, mobildeki yerel bildirim web'e taşınmadı, bkz. Açık Sorular).
- [x] Hedeflerim — ilerleme çubuklu hedef kartları, "Para Ekle" ile `goals.saved_amount` güncelleme + `goal_transactions` kaydı. `savings_pool` havuz akışı bu fazda YOK (doğrudan hedefe ekleme var) — bkz. Açık Sorular.
- [x] Yatırımlarım — mobildeki `AssetType` enum'u birebir portlandı (altın/kripto/döviz/gümüş). **Canlı piyasa fiyatı bağlandı (21 Temmuz 2026)** — mevcut, keyless `get-investment-prices` edge function'ı (Truncgil altın/döviz + CoinGecko kripto) `supabase.functions.invoke` ile çağrılıyor, portföy toplam değeri + kâr/zarar (yeşil/kırmızı, trend ikonlu) gösteriliyor. Fiyat alarmı (`price_alerts`) UI'ı hâlâ bağlanmadı — bkz. Açık Sorular.
- [x] Abonelikler — aylık toplam gider özeti, ekle/duraklat/etkinleştir/sil/düzenle. **Mobil detay-parite geçişiyle genişletildi:** periyot/tarihler/ödeme yöntemi/kart etiketi/durum/bildirim alanları, canlı aylık↔yıllık önizleme.
- [x] Gruplarım — grup oluştur, listele, davet/katılma (`accept_group_invite` RPC + `get_invite_preview`), üye listesi (takma ad, admin yap/çıkar), sohbet, harcama paylaştırma (eşit böl + "Ödedim" kendi-beyanı + tepki/yorum) — bkz. Kararlar Günlüğü 21/22 numaralı maddeler. **Grup Analizi (21 Temmuz 2026, mobilde de YOK — kullanıcı isteğiyle web'e özel yeni bir özellik):** grup detay sayfasına dönem filtreli, sekmeli (Kategori Dağılımı/Genel Kategoriler/Market Analizi) grafikli analiz kartı eklendi — kişisel Analiz sayfasındaki aynı `DonutBreakdown`/`StoreBreakdownChart` bileşenlerini `expense_groups` üzerinden gruba scope ederek yeniden kullanıyor (`getGroupCategoryBreakdown`/`getGroupParentCategoryBreakdown`/`getGroupStoreBreakdown`, `src/lib/data/groups.ts`).
- [x] Analizler & Raporlar — **mobil detay-parite geçişiyle 4 sekmeli yapıya genişletildi:** Harcamalar (dönem filtresi + carousel: Kategori Dağılımı/Genel Kategoriler/Market Analizi + 6 Aylık Trend), Gelir-Gider (ayrı bar + ayrı net bakiye trend grafiği), Abonelikler (özet/insight/trend/timeline), Grup (üye bazlı harcama). Detaylar için Kararlar Günlüğü'ndeki "ikinci detay-parite turu" girdisine bkz.
- [x] AI Sohbet — mobil tarafta zaten var olan, sunucu taraflı ve JWT korumalı `fisle-ai-chat` Supabase Edge Function'ına bağlandı (Gemini key client'a hiç gelmiyor).
- [x] Özet Oluştur (`/reports`) — **AI Sohbet'ten TAMAMEN AYRI, gerçek rapor özelliği** (mobildeki `reports` modülü, AI değil): dönem seçimi (Günlük/Aylık) → `report_service.dart`'taki `generateReport()` ile birebir aynı sorgularla (expenses+items, incomes, fixed_expenses, user_debts, goals, subscriptions) özet ekranı — Gelir/Gider/Net kartları, kategori donut grafiği, borç/alacak, hedefler, abonelikler, ürün detay tablosu. PDF/Excel export yerine tarayıcının native `window.print()`'i kullanılıyor ("Yazdır / PDF Kaydet" butonu) — mobildeki `pdf`/`printing` paket bağımlılığı web'e taşınmadı, bkz. Açık Sorular. Dashboard'daki "Özet Oluştur" butonu `/reports`'a yönlendiriyor (düzeltme: ilk yazımda yanlışlıkla AI Sohbet'e yönlendirilmişti).
- [x] Notlar — tam sayfa not ızgarası, renk seçimi, tamamlandı işaretleme, silme. `user_notes` (text PK).
- [x] Ayarlar — Profil (ad düzenleme, gerçek `users.name` update), Görünüm (tema seçici), Esnaf Modu'na geçiş kartı. Bildirimler mobilde cihaz-yerel olduğu için web'e henüz taşınmadı (bilgilendirme notu var). **21 Temmuz 2026:** Hesap bölümü eklendi (`AccountDangerZone`) — Çıkış Yap, Verileri Sıfırla (mobildeki `resetAllUserData()`'nın 16 tablosu birebir, çift onay), Hesabı Sil (mevcut `delete_current_user` SECURITY DEFINER RPC). Premium'a Geç / Esnaf Aboneliğine Geç / Bir İşyerine Katıl henüz YOK — bkz. Açık Sorular.

---

## Faz 3 — Esnaf Modu

Mobilde de önce ortak altyapı, sonra sektörel modüller sırasıyla inşa edilmişti (bkz. proje hafızası) — web'de aynı sıra izlendi.

- [x] **Ortak Esnaf Altyapısı** (bu fazda tamamlandı):
  - İşletme kurulum formu (`/esnaf`) — sektör seçimi (mobildeki `BusinessSectors.all` ile birebir 6 sektör), işletme adı/vergi no/telefon/KDV oranı. `businesses` tablosuna `sector` + `business_type` (sektöre göre otomatik türetiliyor) yazıyor.
  - Aktif işletme mekanizması — mobil `active_business_provider.dart`'ın (SharedPreferences) web karşılığı: `fisstech-active-business` cookie'si + Server Action (`src/lib/esnaf/active-business.ts`). Birden fazla işletmesi olan kullanıcılar için üstte switcher.
  - Esnaf alt-navigasyonu (Pano/Kasa Defteri/Faturalar/Personel/Stok/Raporlar) — `/esnaf/(dashboard)` route group'u altında, ana Sidebar'ın içinde (mobildeki gibi tamamen ayrı bir shell değil, tek sayfa uygulaması içinde ikinci seviye tab bar).
  - **Pano** — bu ay ciro/gider/kâr özeti + son işlemler.
  - **Kasa Defteri** — gelir/gider hızlı giriş, `business_service_chips` ile hızlı fiyat seçimi, KDV inclusive hesaplama (mobildeki gibi `tutar × oran / (100+oran)`).
  - **Faturalar** — giden/gelen ayrımı, KDV exclusive hesaplama (`tutar × oran / 100`), ödendi işaretleme.
  - **Personel & Maaş** — çalışan ekle/sil, `ensureSalaryRecords()` ile her ay otomatik maaş kaydı oluşturma (mobildeki `EmployeeService.ensureSalaryRecords()` ile aynı mantık), ödendi toggle.
  - **Stok** — ürün ekle/sil, giriş/çıkış/sayım hareketleri ile `current_qty` atomik güncelleme, kritik seviye rozeti.
  - **İşletme Raporları** — son 6 ay ciro/gider trend grafiği, bu ay KDV özeti (tahsil/ödenen/net), gider kategorisi kırılımı.
  - **Bugünkü Ciro kartı** (Kasa Defteri'nde) — mobildeki Kafe/Restoran "Kasa & Tahsilat" ekranının hero kartıyla aynı fikir (bugünkü ciro + hesap/işlem sayısı), kullanıcı geri bildirimiyle eklendi. `getDailyTotals()` — sadece bugünün `business_incomes`/`business_expenses` kayıtlarını toplar.
  - **Evrak Arşivi** (`/esnaf/evrak`, TÜM sektörler) — çoklu belge yükleme + seri tarama: her dosya `scan-receipt` edge function'ına (Fiş Tara'daki AYNI, zaten deploy edilmiş fonksiyon) gönderilir, sonuç (mağaza/tarih/tutar) fatura taslağına (karşı taraf/tarih/tutar) eşlenir, kullanıcı düzenleyip toplu kaydeder. Görseller `receipts` bucket'ına yüklenir, `invoices.image_url` alanına yazılır (mobil şemada ayrı bir "documents" tablosu yok — evrak arşivi = görseli olan faturalar). **Not:** `scan-receipt` fiş için optimize edilmiş bir prompt kullanıyor, genel evrak/fatura OCR'ı için özel tasarlanmamış — pratikte mağaza adı/tarih/tutar çoğu belgede makul çıkarılıyor ama mükemmel değil; ileride evraka özel bir edge function daha doğru olur (bkz. Açık Sorular).
  - **Menü Yönetimi** (`/esnaf/menu`, SADECE Yeme & İçme/`business_type='kafe'`) — kategori + ürün CRUD, `031_kafe_restoran.sql`'deki `menu_categories`/`menu_items` tablolarına yazıyor. Diğer sektörlerde bu sekme gösterilmiyor (nav sektöre göre koşullu) ve direkt URL ile girilirse "yakında" mesajı gösteriyor (masa/adisyon/sipariş gibi kafe'nin diğer özellikleri henüz yok, bkz. Açık Sorular).
- [ ] **Premium gating uygulanmadı** — mobilde Esnaf Modu `users.esnaf_plan === 'esnaf_premium'` ile kilitli; web'de henüz ödeme/premium akışı olmadığından herhangi bir giriş yapmış kullanıcı erişebiliyor. Bilinçli bir kapsam kararı, bkz. Açık Sorular.
- [x] Modül 1 — Hizmet & Bakım *(kısmi — Müşteriler, Hizmet Kataloğu, Ajanda (günlük 30dk slot takvimi), Atölye (Bekliyor/Devam/Hazır Kanban), 3 adımlı Hizmet Sihirbazı, WhatsApp "iş tamamlandı" bildirimi var; sektöre özel Pano (bakım hatırlatmaları, usta primleri) yok — bkz. Açık Sorular)*
- [x] Modül 2 — Hızlı Perakende *(kısmi — Ürünler/Kategoriler, Hızlı Kasa/POS, Veresiye Defteri var; Gün Sonu Fiş Tarama, Perakende Giderleri, Satış Geçmişi, Ayarlar, PLU kod arama, varyasyonlar yok — bkz. Açık Sorular)*
- [x] Modül 3 — Yeme & İçme *(kısmi — Menü Yönetimi var; masa/adisyon/sipariş/paket servis yok)*
- [x] Modül 4 — Yüksek Hacimli Satış *(kısmi — Portföy (kategori+durum CRUD), Müşteriler, Satış Sihirbazı (müşteri/misafir → portföy/manuel ürün → tek çekim/taksitli ödeme + PDF makbuz indirme), Süreçler (Kaporalı/Taksit/Tamamlanan) var; Evrak Arşivi (`sale_documents`) mobilde de placeholder olduğu için taşınmadı — bkz. Açık Sorular)*
- [x] Modül 5 — Toptancı & İmalatçı *(kısmi — Depo & Stok (kritik seviye uyarısı + stok düzelt), Bayiler (risk seviyesi + kredi limiti + cari hesap detayı + tahsilat), Toplu Sipariş (bayi + ürün sepeti + iskonto), Sevkiyat Kanban (Bekliyor/Hazırlanıyor/Yolda, Teslim Edildi'de otomatik cari borç işleme) var — bkz. Açık Sorular)*
- [x] Modül 6 — Serbest Meslek & Proje *(kısmi — Müşteriler, Projeler (durum filtresi + ilerleme çubuğu), Proje Sihirbazı (Tek Seferlik/Aşama Aşama ödeme planı), Proje Detay (hakediş/görevler/masraflar), Görevler (çapraz-proje + basit kronometre), Finans (tahsil/bekleyen + müşteri bazlı bakiye) var — bkz. Açık Sorular)*

---

## Faz 4 — Senkronizasyon & Entegrasyon

- [x] Mobil ile ortak backend/API bağlantısı kuruldu (aynı Supabase projesi, `@supabase/ssr`)
- [x] Fiş tarama AI pipeline'ı (`scan-receipt` Supabase Edge Function, Gemini 2.5 Flash) web'den tetiklenebiliyor
- [x] AI Sohbet pipeline'ı (`fisle-ai-chat` Supabase Edge Function, Gemini 2.5 Flash + function calling) web'den tetiklenebiliyor
- [x] **Gerçek zamanlı senkronizasyon** — `useRealtimeRefresh` hook'u (`src/lib/utils/useRealtimeRefresh.ts`) + `<RealtimeRefresh tables={[...]} />` wrapper'ı, `(app)/layout.tsx`'te 11 kişisel tablo ve `esnaf/(dashboard)/layout.tsx`'te 21 esnaf tablosu için Supabase Postgres Changes'e abone oluyor, herhangi bir değişiklikte (debounce'lı) `router.refresh()` tetikliyor. **Çalışması için `docs/sql/047_web_realtime.sql`'in Supabase Dashboard → SQL Editor'da çalıştırılması ŞART** — mobil taraf sadece `group_messages`/`expense_groups`/kafe-staff tablolarını `supabase_realtime` publication'ına eklemişti, web'in ihtiyaç duyduğu geri kalan ~30 tablo bu migration'la ekleniyor; migration çalıştırılmadan abonelikler sessizce hiçbir olay almaz (asla sahte bir "canlı" görünüm üretmez, sadece no-op kalır). RLS zaten Realtime'ı da kapsıyor, bu yüzden client tarafında `user_id`/`business_id` filtresi GEREKMİYOR.
- [x] Esnaf modülü seçimi — aktif işletme cookie (web) / SharedPreferences (mobil) her cihazda kendi bağlamını hatırlıyor; bu KASITLI bir tasarım (çoğu kullanıcı senaryosunda beklenen davranış), ayrıca senkronize edilmesi gerekmiyor — eski not (Esnaf Modu web'e gelmeden önce yazılmıştı) artık geçerli değildi, kaldırıldı.

**Önemli mimari not (Esnaf-lokal-OCR için hâlâ geçerli):** Mobildeki Esnaf-lokal-OCR özelliği Gemini'yi `google_generative_ai` paketiyle **doğrudan client'tan, gömülü bir API key ile** çağırıyor. Bu pattern web'de KULLANILMAMALI. AI Chat için bu artık bir sorun değil — mobilde de aslında sunucu taraflı `fisle-ai-chat` edge function'ı zaten mevcuttu (mobil client'ın onu kullanmıyor olması ayrı bir konu), web bunu doğrudan kullanıyor. Esnaf Modu fazına gelindiğinde OCR için benzer bir sunucu taraflı çözüm (yeni Edge Function veya Route Handler) tasarlanmalı.

---

## Faz 5 — Cilalama & Yayına Hazırlık

- [x] Responsive test (mobil ~390px / tablet ~768px / masaüstü ~1440px) — auth ekranlarında Playwright ile, Dashboard'da gerçek kullanıcı hesabıyla doğrulandı
- [x] Erişilebilirlik kontrolü (klavye odağı, kontrast, azaltılmış hareket) — renk kontrastları hesaplanarak doğrulandı, `prefers-reduced-motion` desteği eklendi, 20 Temmuz 2026'da tam bir a11y taraması yapıldı (bkz. Kararlar Günlüğü): eksik `aria-label`'lar, paylaşılan `Dialog` bileşenine focus trap/restore, skip-to-content linki, `aria-current="page"`, nav landmark'ları.
- [x] Performans/yükleme hızı kontrolü — 20 Temmuz 2026'da yapıldı (bkz. Kararlar Günlüğü): `xlsx`/`jspdf` lazy-load'a çevrildi, gereksiz sequential sorgu bulunan `/groups/[id]` paralelleştirildi, gereksiz client component taraması yapıldı (hiçbiri bulunamadı — mevcut "use client" kullanımlarının hepsi gerekçeli).
- [x] Deployment hazırlığı — 20 Temmuz 2026'da kontrol edildi (bkz. Kararlar Günlüğü): `.env.example`/`.gitignore` doğrulandı (kullanılan tüm env değişkenleri kapsanıyor), Next.js zero-config Vercel dağıtımı için `vercel.json`'a gerek olmadığı teyit edildi. **Gerçek dağıtım (Vercel hesabı/proje oluşturma) henüz yapılmadı** — dışa açık/geri alınması zor bir adım olduğu için kullanıcı onayı bekleniyor.
- [ ] Gerçek içerik/metin geçişi
- [ ] Yayına alma

---

## Faz 6 — Pazarlama Sitesi (Landing Page) & Marka Rebrand

- [x] Site geneli renk rebrand: Gül (#B23A65) → Yeşil (#1E4D2B açık / #4CAF50 koyu) — `styles/tokens.css`, WCAG AA kontrast hesaplanarak
- [x] `/` herkese açık pazarlama sayfasına çevrildi (middleware `PUBLIC_ROUTES` + `page.tsx`)
- [x] Yatay logo (SVG, `currentColor`) + responsive Navbar (tema geçişi, oturum-duyarlı CTA, mobil menü)
- [x] Hero bölümü (canlı fiş-önizleme mockup'ı, scan-frame motifi)
- [x] Ücretsiz Fiş Tarama demosu — SİMÜLE (gerçek OCR çağrısı yok, kullanıcı kararı), localStorage ile 5 hak sınırı
- [x] Nasıl Çalışır (3 adım), Özellikler (4 kart + 1 hafta ücretsiz banner)
- [x] Fiyatlandırma (gerçek RevenueCat plan/fiyat verisiyle, aylık/yıllık toggle)
- [x] SSS accordion (yeni bir primitive — projede önceden yoktu)
- [x] AI Destek Widget + yeni `src/app/api/landing-chat` route (public, `fisle-ai-chat`'ten bağımsız — `GEMINI_API_KEY` env değişkeni gerektiriyor, henüz sağlanmadı, bkz. Açık Sorular)
- [x] Scroll-reveal + hover mikro-etkileşimler — bağımlılıksız (`Reveal`, IntersectionObserver), yeni paket eklenmedi
- [x] Responsive doğrulama (390/768/1440, açık/koyu) — Playwright ile ekran görüntüsü + gerçek tıklama/etkileşim testi (SSS accordion aç, fiyat aylık/yıllık toggle, tarama demosunu 5 hakkın hepsini tüketene kadar çalıştırma, AI widget açma) — sıfır konsol hatası
- [x] Yatay logo, elle çizilmiş SVG yerine kullanıcının sağladığı gerçek PNG'lerle (`public/logo-f-icon.png`, `public/logo-wordmark.png`) değiştirildi — bkz. Kararlar Günlüğü
- [x] framer-motion eklendi; Step Slider (otomatik+tıklanabilir 4 adım, orbital halka + glassmorphism simülasyon), "Neden Fişştech?", İş Akışı/Analiz yüzen kartlar, iki kolonlu "Merak Edilenler" (SSS v2), Kapanış CTA, kapsamlı Footer (+ 6 yasal/destek stub rotası) eklendi
- [x] Örnek verilerdeki rakip-site izlenimi veren placeholder'lar ("Migros" vb.) "Kampüs Market" ile özgünleştirildi
- [x] Fiyatlandırma kartları: tüm kartlarda hover glow, yıllık/aylık şeffaf fiyat mantığı, emoji yerine lucide ikon, Bulut Yedekleme'nin gerçek iş kuralını yansıtan açıklaması
- [x] "Sektörel Çözümler" — 6 gerçek Esnaf Modu sektörünü tanıtan interaktif sekme bölümü (Nasıl Çalışır?'den hemen sonra)
- [x] "Neden Fişştech?"teki placeholder görsel kaldırıldı, yerine tamamen kod-tabanlı (CSS + framer-motion) 3D yüzen fiş simülasyonu
- [x] Pricing içeriği mobil uygulamayla senkronize edildi (özellik listeleri + Bulut Yedekleme'nin gerçek iş kuralı), "Planları Karşılaştırın" tablosu eklendi
- [x] Raporlar sayfası: Market Dağılımı grafiği, Hedefler ilerleme çubuğu, print-özel tablo görünümü + `@media print` düzeltmeleri (scrollbar/URL-footer/tema-bağımsız açık çıktı) — bkz. Doğrulama sınırı notu (gerçek oturumla henüz görsel teyit edilmedi)
- [x] Uygulama içi navigasyon: Sidebar'a "Ana Sayfaya Dön", Topbar'a "Profilim"/"Tanıtım Sitesine Dön" — bkz. Doğrulama sınırı notu
- [ ] Mobil (Flutter) tarafının bu yeni yeşil/mint marka kimliğine göre güncellenmesi — kullanıcı "şimdilik sadece web" dedi, henüz yapılmadı (mobilde zaten "Yeşil" adında bir tema olarak var, sadece varsayılan/tek tema yapılması gerekebilir — ayrı bir karar)

---

- **23 Temmuz 2026 (Faz 8 devamı — madde 3: /api/ai-chat rate limit + mesaj uzunluğu sınırı):** Güvenlik denetiminde bulundu: `landing-chat/route.ts` (anonim, herkese açık widget) IP-bazlı bir rate limiter (`10 istek/10dk`) + `MAX_MESSAGE_LENGTH=500` içeriyordu, ama `ai-chat/route.ts` (kimlik doğrulamalı, gerçek finansal veri özetleyen, çok daha maliyetli asıl özellik) hiçbir sınırlamaya sahip değildi — herhangi bir hesap, her çağrısı gerçek Gemini API maliyeti doğuran bu endpoint'i sınırsız hızda çağırabilirdi. **Düzeltme:** landing-chat'in limiter mantığı `lib/utils/rate-limit.ts`'e (`createRateLimiter(max, windowMs)`) taşınıp iki route'un da ortak kullanacağı tek kaynağa dönüştürüldü (kod tekrarı da ortadan kalktı — Optimizasyon.md'nin "duplicated utility code" maddesiyle örtüşen bir bonus). `ai-chat` route'una `user.id` bazlı rate limit (kimlik doğrulaması zaten var, IP yerine kullanıcı bazlı daha isabetli) — `20 istek/10dk` — ve `MAX_MESSAGE_LENGTH=4000` (hem tekil mesaj hem geçmiş sohbet öğeleri için) eklendi. **Not:** `src/app/api/ai-chat/route.ts` ve `src/lib/ai-chat/client.ts` bu oturumda başka, henüz commit edilmemiş bir çalışmanın parçası (git'te untracked/modified olarak bulundu) — düzeltme diskteki dosyaya uygulandı ve çalışıyor, ama o dosyalar bu commit'e dahil edilmedi, ait olduğu iş kendi commit'ini beklesin diye.
- **23 Temmuz 2026 (Faz 8 devamı — madde 2: Ekip mutasyonlarına sahiplik yeniden-doğrulaması):** Güvenlik denetiminde bulundu: `esnaf-team.ts`'teki `cancelInvite`/`updateStaffRole`/`removeStaffMember` sadece `.eq("id", ...)` ile RLS'e güveniyordu, sunucu tarafında hiçbir sahiplik kontrolü yoktu — `updateStaffRole(supabase, herhangiBirStaffId, "yardimci_yonetici")` gibi export edilmiş bir fonksiyon dışarıdan (ör. tarayıcı konsolu) herhangi bir `staffId` ile çağrılabilirdi. Kullanıcıya `updateStaffRole`'ün kaldırılması mı yoksa güvenli hale getirilip saklanması mı sorulduğunda "güvenli hale getir" seçildi (UI'da henüz kullanılmasa da ileride rol-değiştirme eklenmek istenirse hazır ve güvenli olsun diye). **Düzeltme:** yeni `assertIsBusinessOwner()` yardımcı fonksiyonu — `businesses.user_id === auth.uid()` kontrolünü açıkça yapıp anlamlı bir hata fırlatıyor (RLS zaten aynı şeyi engelliyor olsa bile, ikinci bir savunma katmanı). Üç mutasyon fonksiyonu da artık `businessId` parametresi alıyor, önce bu kontrolden geçiyor, sonra `.eq("id", ...).eq("business_id", businessId)` ile çift filtreli çalışıyor. `team-roster.tsx`'teki çağrı yerleri (`removeStaffMember`, `cancelInvite`) zaten prop olarak sahip olduğu `businessId`'yi geçecek şekilde güncellendi.
- **22 Temmuz 2026 (Faz 8 başlıyor — Güvenlik + Optimizasyon denetimi, madde 1: dosya yükleme sertleştirmesi):** Kullanıcı `Güvenlik ve Optimizasyon/guvenlik.md` + `Optimizasyon.md` dosyalarını proje standardı olarak benimsetip web koduna tam bir güvenlik+optimizasyon denetimi yaptırdı (bulgular ayrı raporlarda sunuldu, plan onaylandı). En kritik bulgu: `uploadBusinessLogo()` (`lib/esnaf/business-logo.ts`) ve `uploadReceipts()` (`lib/scan/saveExpense.ts`) dosya tipini SADECE attacker-controlled `file.name` uzantısı ve `file.type`'a göre belirliyordu, gerçek içerik hiç doğrulanmıyordu — `business_logos` bucket'ı PUBLIC olduğundan (herkese açık URL, auth gerekmiyor), kötü niyetli bir kullanıcı `image/svg+xml` content-type'lı, içine script gömülü bir dosyayı ".jpg" adıyla yükleyip herkese açık bir URL üzerinden dağıtabilirdi. **Düzeltme:** yeni `lib/utils/file-validation.ts` — `detectImageType()`, dosyanın ilk 12 baytına (magic number) bakarak sadece gerçek JPEG/PNG/WebP'ye izin veriyor, uzantı/MIME adına hiç güvenmiyor. Her iki upload fonksiyonu da artık bu doğrulamadan geçmeyen dosyaları reddediyor (`business-logo.ts` hata fırlatıyor — kullanıcı zaten görüyor; `saveExpense.ts` mevcut "upload hatasında sessizce atla" davranışıyla tutarlı kalması için sessizce atlıyor), ve Storage'a yüklerken `Content-Type`'ı client'ın bildirdiği değer yerine doğrulanmış, sabit MIME tipiyle (`{ contentType: detected.mime }`) gönderiyor.
- **22 Temmuz 2026 (Faz 7 acil düzeltme — get_my_businesses() henüz çalıştırılmamışken TÜM mevcut Esnaf Modu sahiplerinin işletmeleri kayboluyordu):** Kullanıcı canlıda 6 gerçek işletmesi olmasına rağmen Esnaf Modu'na girince "İşletmeni Kur" (sıfır işletme) ekranını gördüğünü bildirdi. Kök neden: bir önceki turda `getUserBusinesses()` (`lib/esnaf/active-business.ts`) `get_my_businesses()` RPC'sini çağıracak şekilde değiştirilmişti, ama bu RPC'yi oluşturan `docs/sql/050_my_businesses.sql` henüz Supabase SQL Editor'da ÇALIŞTIRILMAMIŞTI — RPC canlıda yok, çağrı hata döndü, ve kod hatayı hiç kontrol etmeden `data ?? []` ile sessizce boş diziye düşüyordu. Sonuç: SADECE Ekip özelliği değil, TÜM esnaf sahiplerinin `getActiveBusiness()`'ı null dönüyor, `(dashboard)` layout'u herkesi `/esnaf`'a (İşletmeni Kur ekranına) geri yönlendiriyordu — geriye dönük regresyon. **Düzeltme:** `getUserBusinesses()` artık RPC hatasını kontrol ediyor; hata varsa (fonksiyon henüz deploy edilmemiş) eski, sadece-sahip-olunan-işletmeler sorgusuna (`businesses.eq("user_id", ...)`) sessizce geri düşüyor — SQL dosyası çalıştırılana kadar sahiplerin işletmeleri hiç kaybolmuyor (sadece Ekip üzerinden katılan personelin işletmeleri bu fallback'te görünmez, o da zaten SQL çalıştırılmadan hiç kullanılamayan bir özellik). **Önemli:** `docs/sql/050_my_businesses.sql` yine de çalıştırılmalı — aksi halde davet kabul eden personel hâlâ işletmeyi göremez, ama artık en azından mevcut sahiplerin verisi asla etkilenmiyor.
- **22 Temmuz 2026 (Faz 7 tamamlandı — Ekip: davet kodlu gerçek-hesap personel + kota, mobil paritesi turu son maddesi):** Kullanıcı ek olarak istedi: "davet kodu ile personel ekleme... 10 personel hakkı vardır... manuel eklenen personeller bu kotayı etkilemez." `employees` (manuel Personel Not Defteri) zaten tam çalışıyordu — bu turda sıfırdan inşa edilen SADECE gerçek-hesaplı "Ekip" (mobildeki workspace/`business_staff`+`business_invites` karşılığı, web'de daha önce hiç yoktu).
  - **Canlı DB doğrulaması (zorunlu ön adım, atlanmadı):** `npx supabase gen types typescript --project-id tmxcldexspjawfculpmg` ile canlı `fisle` projesi sorgulandı. `business_staff`/`business_invites` tabloları, `accept_business_invite` RPC'si, HEM `is_business_member` HEM `is_business_staff` yardımcı fonksiyonları canlıda doğrulandı. **Şema sapması bulundu:** `business_staff`'ta hiçbir migration dosyasında geçmeyen bir `can_chat: boolean` kolonu canlıda mevcut — tipler buna göre güncellendi (mobil migration dosyaları burada da güncel değilmiş, `can_chat` muhtemelen SQL Editor'dan elle eklenmiş, tıpkı daha önce keşfedilen `staff_commissions` tablosu gibi).
  - **Kritik, planlanmamış mimari boşluk keşfedilip düzeltildi:** `getActiveBusiness()`/`getUserBusinesses()` (`lib/esnaf/active-business.ts`) SADECE `businesses.user_id = auth.uid()` (sahip) filtresiyle sorgu yapıyordu — ve `businesses` tablosunun RLS SELECT politikası da (`026_esnaf_modu.sql`, hiç güncellenmemiş) sadece sahibi kapsıyor. Yani bir personel daveti kabul edip `business_staff`'a girse bile, dashboard hâlâ o işletmeyi HİÇ GÖREMEZ ve `/esnaf`'a geri yönlendirilirdi — davet-kabul akışı sessizce kırık kalırdı. Çözüm: `docs/sql/050_my_businesses.sql` ile `get_business_invite_preview`/`get_invite_preview` ile aynı desende (`SECURITY DEFINER`, dar kapsamlı) bir `get_my_businesses()` RPC'si eklendi — sahip olunan + `business_staff` üzerinden aktif (left_at IS NULL) olunan işletmeleri birleştirip döndürüyor. `active-business.ts` bu RPC'yi kullanacak şekilde yeniden yazıldı (davranışı sahipler için AYNI kalıyor, personel için de artık çalışıyor).
  - **Tipler + data layer:** `BusinessStaffRow`/`BusinessInviteRow` (`lib/types/esnaf.ts`), yeni `lib/data/esnaf-team.ts` (`getActiveStaffCount`, `getActiveStaff`, `getPendingInvites`, `createStaffInvite`, `cancelInvite`, `getStaffInvitePreview`, `acceptStaffInvite`, `updateStaffRole`, `removeStaffMember` — hepsi `lib/data/groups.ts`'in davet/üye desenleriyle aynı stil). Opsiyonel önizleme RPC'si (`docs/sql/051_business_invite_preview.sql`, `get_invite_preview` ile birebir aynı desen) Gruplarım'daki UX tutarlılığı için eklendi. `(dashboard)/layout.tsx`'teki `ESNAF_REALTIME_TABLES`'a `business_staff`/`business_invites` eklendi.
  - **Kota mantığı (kullanıcı talebi tam karşılandı):** `getActiveStaffCount()` SADECE `business_staff` (left_at IS NULL) sayar, `employees`'e asla dokunmaz — iki sayaç kod seviyesinde de tamamen ayrık. Limit `users.esnaf_staff_limit` kolonundan okunuyor (mobil RevenueCat webhook'u dolduruyor, web sadece okur, hiç yazmaz — satın alma zaten sadece mobil üzerinden yapılıyor).
  - **RLS asimetrisi UI'a yansıtıldı:** `business_staff`'ın SELECT politikası personel için sadece KENDİ satırını döndürür, tam roster'ı sadece işletme sahibi görebilir. `/esnaf/ekip` sayfası bunu `business.user_id === kullanıcı.id` kontrolüyle ayırıyor — sahip tam roster + "Davet Kodu Oluştur" (`TeamRoster`/`TeamInviteDialog`) görüyor, personel sadece bilgilendirme mesajı görüyor.
  - **UI:** `esnaf-sub-nav.tsx`'in `BASE_TABS`'ında mevcut "Personel" sekmesinin hemen yanına "Ekip" (`UserPlus` ikonu) eklendi — kullanıcının "Personel'in yanına Ekip" talebi birebir karşılandı. Davetli tarafı için `/esnaf` giriş sayfası (`EsnafEntryTabs`, yeni) artık "İşletmeni Kur" / "Davetiye ile Katıl" (`JoinBusinessForm`, kod gir → önizleme → onayla → `acceptStaffInvite` → `setActiveBusinessId` → `/esnaf/pano`) arasında sekmeli — Gruplarım'ın `JoinGroupCard`'ıyla aynı önizle-sonra-onayla deseni.
  - **Doğrulama sınırı:** Gerçek kimlik doğrulama + iki ayrı test hesabı (sahip + personel) gerektirdiğinden bu ortamda uçtan uca canlı test yapılamadı — `tsc`/`lint`/`build` temiz, RLS/şema canlıdan doğrulandı, kod incelemesi yapıldı. Kullanıcının **`docs/sql/050_my_businesses.sql` ve `docs/sql/051_business_invite_preview.sql` dosyalarını Supabase SQL Editor'da çalıştırması gerekiyor** — çalıştırılmadan Ekip özelliği (özellikle davet kabul akışı) canlıda çalışmaz.
- **22 Temmuz 2026 (Faz 7 devamı — madde 1/6: AI tarama prompt'una fatura/banka ekstresi/resmi belge kuralları, DEPLOY YAPILMADI):** `scan-receipt` Supabase Edge Function'ının gerçek kaynağı fiziksel olarak `fisleapp` (mobil, SALT OKUNUR) klasöründe duruyor, mobil ve web AYNI canlı Supabase projesini (`fisle`, ref `tmxcldexspjawfculpmg`) paylaşıyor. Bu yüzden `Fisstech_web/supabase/functions/scan-receipt/index.ts` adında web'e ait YENİ bir kopya oluşturuldu — mobil kaynaktaki gerçek prompt (`fisle_app/supabase/functions/scan-receipt/index.ts`) birebir okunup kopyalandı, TİP A (taksit tablosu)/TİP B (normal fiş) yapısına uyumlu yeni bir **TİP C** bloğu eklendi: fatura → tek kalem, kategori "Faturalar"; banka ekstresi → tek kalem "Banka Ekstresi", kategori "Faturalar", tutar = toplam borç; sözleşme/resmi belge → tutar 0, kalem adı = belge başlığı, `store_name` = karşı taraf. Kategori eşleşmesi zaten `buildCategorySection()` ile güvenli şekilde "Diğer"e düşüyor, ek istemci değişikliği gerekmedi. **Kullanıcı kararıyla (AskUserQuestion ile onaylandı) `supabase functions deploy` ÇALIŞTIRILMADI** — bu paylaşılan prod backend'i hem mobil hem web kullanıcılarını aynı anda etkileyeceğinden, canlıya alma adımı kullanıcıya bırakıldı (`supabase link` + `supabase functions deploy scan-receipt`, CLI bu ortamda zaten authenticated). **Yan iş:** Deno runtime'ı hedefleyen bu dosya (`Deno.env`, uzak URL import'ları) Next.js'in `tsc`/`eslint` taramasına girmesin diye `tsconfig.json`'a `exclude: ["supabase/functions/**"]` ve `eslint.config.mjs`'e aynı `globalIgnores` girdisi eklendi — aksi halde build/lint bu dosyada Deno tiplerini bulamayıp hata verirdi.
- **22 Temmuz 2026 (Faz 7 devamı — madde 4/6: Raporlar sayfasına 2 yeni grafik):** `/esnaf/raporlar`'a iki değişiklik: **(1) Gider kategorisi donut'u** — "Bu Ay Gider Kategorileri" düz bir `<ul>` listesiydi; kişisel Analiz sayfasında zaten var olan `CategoryDonutChart` (`components/modules/analytics/`) ve `CHART_COLORS` paleti (`lib/data/analytics.ts` — önceden bilinmiyordu, `store-breakdown-chart.tsx` içinde aynı renklerin ikinci bir kopyası inline duruyordu; yeni bir `chart-colors.ts` dosyası açmak yerine bu mevcut tek doğruluk kaynağı reuse edildi) doğrudan reuse edildi, veri zaten mevcut `getExpensesByCategory()`'den geliyor. **(2) Kalem bazlı gelir zaman çizelgesi (yeni)** — mobildeki `get_product_sales`/`get_daily_sales_trend` RPC'leri sadece `order_items` (kafe/restoran) üzerinden çalışıyor, TÜM esnaf sektörlerinde (hizmet/toptan/freelance/satis'ta bu tablo hiç dolmuyor) karşılığı yok. Onun yerine her sektörde gerçekten dolu olan tek tablo `business_incomes` (`chip_label`/`description`/`amount`/`transaction_date`) kullanılarak yeni `getIncomeItemTrend()` (`lib/data/esnaf.ts`) yazıldı — gün × üst-5 gelir kalemi (gerisi "Diğer"), yeni `IncomeItemTrendChart` bileşeni (recharts yığılmış `BarChart`, mevcut grafik konvansiyonlarıyla aynı stil) ile gösteriliyor. Bazı sektörlerde (ör. freelance, gelir akışı `business_incomes`'a düşmeyen bir modülse) grafik boş çıkabilir — bu uydurma veri koymamak için bilinçli, doğru davranış. `BalanceTrendChart` (Kar/Zarar + Gelir/Gider) zaten vardı, dokunulmadı. **Doğrulama sınırı:** `/esnaf/raporlar` kimlik doğrulama + aktif işletme gerektiriyor, bu ortamda gerçek bir test hesabı olmadığından grafiklerin gerçek veriyle dolu görünümü tarayıcıda GÖRSEL olarak doğrulanamadı (`tsc`/`lint`/`build` temiz, kod incelemesi yapıldı) — kullanıcının kendi hesabıyla son bir kontrolü öneriliyor.
- **22 Temmuz 2026 (Faz 7 devamı — madde 3/6: Fiş/gider formuna dosya eki):** `ExpenseFormDialog`'a (manuel harcama ekle/düzenle) opsiyonel "Fiş/Belge" bölümü eklendi. Sıfırdan yeni kod yazılmadı — AI-tarama akışı için zaten var olan `DropzoneUploader` (`components/modules/scan/`) ve `uploadReceipts()` (`lib/scan/saveExpense.ts`, private `receipts` bucket'ına yükleyip 1 yıllık imzalı URL döndürüyor) birebir reuse edildi. Düzenleme modunda mevcut `expense.receipt_image_url` (virgülle ayrılmış URL string, mobil formatıyla uyumlu) parse edilip listelenir, kaldırılabilir; yeni dosyalar eklenip kaydedilince eski+yeni URL'ler birleştirilip hem insert hem update payload'ına yazılır. **Bilinçli kapsam dışı:** Storage'dan dosya silme — mobilde de yok, kaldırma sadece referansı formdan çıkarıyor, objeyi silmiyor. Kategori dropdown'ındaki "Özel Kategori Ekle" zaten önceki bir turda inşa edilmişti (`CategoryAddInline`), bu turda dokunulmadı.
- **22 Temmuz 2026 (Faz 7 devamı — madde 2/6: Yüksek Hacimli Satış için Hızlı Kasa):** Mobilde `yuksek_hacim` sektörü (Emlak/Galeri/Kuyumcu) ayrı bir PLU ekranına sahip değil — aynı paylaşılan Hızlı Kasa ekranını (perakende ile ortak) kullanıyor. Web tarafında bu ekran (`PerakendePos`, PLU kod input'u dahil tam çalışır durumda) zaten `/esnaf/perakende/kasa`'da mevcuttu, sadece `satis` sektörünün nav sekmelerinde (`esnaf-sub-nav.tsx`) bir bağlantısı yoktu — mobildeki cross-sector reuse deseniyle birebir aynı şekilde `SECTOR_TABS.satis`'e "Hızlı Kasa" sekmesi eklendi (yeni sayfa YOK, tek satırlık nav değişikliği).
- **22 Temmuz 2026 (Faz 7 — mobil-web özellik paritesi turu başlıyor, madde 6/6: sektör ikonları emoji → lucide-react):** `BUSINESS_SECTORS` (`lib/types/esnaf.ts`) artık emoji yerine mobildeki `lucide_icons_flutter` seçimleriyle birebir eşleşen (aynı Lucide ikon seti) `icon: LucideIcon` alanı taşıyor: Hizmet&Bakım→`Wrench`, Hızlı Perakende→`ShoppingBag`, Yeme&İçme→`UtensilsCrossed`, Yüksek Hacimli Satış→`Gem`, Toptancı&İmalatçı→`Warehouse`, Serbest Meslek→`Briefcase`. `business-setup-form.tsx`'teki sektör seçim grid'i ve `business-switcher.tsx`'teki işletme göstergesi güncellendi. **Teknik not:** `business-switcher.tsx`'te önce `const Icon = sectorIcon(...)` değişkenine atayıp `<Icon/>` şeklinde render etmek React Compiler'ın `react-hooks/static-components` kuralına takıldı ("Cannot create components during render") — düzeltme, projede zaten `esnaf-sub-nav.tsx`'in `<tab.icon/>` deseninde kullandığı doğrudan member-access JSX'e (`<sector.icon/>`) geçmek oldu, ayrı bir değişkene atama yapılmadı. Ayrıca native `<select><option>` SVG/React bileşeni render edemediğinden çoklu-işletme dropdown listesindeki emoji kaldırıldı (düzeltilecek bir eksiklik değil, HTML kısıtı) — tekli işletme görünümünde ikon korunuyor. Uygulama içi ayrı bir paywall/premium sayfası olmadığı doğrulandı (public landing zaten önceki turda `Crown`/`Store`/`Users` kullanıyor) — bu madde için ek iş gerekmedi.
- **22 Temmuz 2026 (Faz 6 devamı — "Anında Finansal Raporlara Dönüşüm" adımının görseli tam bir SaaS dashboard'ına yükseltildi):** Sade, gri ağırlıklı tek-renk halka yerine: (1) 4 kategorili çok renkli donut — Market marka rengiyle (`var(--color-accent)`, tema-uyumlu), Akaryakıt/Yemek/Diğer ise `analytics.ts`'teki `CHART_COLORS` ile aynı gerekçeyle (kategorik ayrım, marka rengine bağlı kalmak okunabilirliği düşürürdü) sabit canlı tonlar (`#3B82F6`/`#A855F7`/`#F97316`); (2) framer-motion'ın imperative `animate()` fonksiyonu + `useInView` hook'uyla, görünüme girince 0'dan gerçek toplama (₺2.450,00, `formatCurrency` ile) sayan bir rakam sayacı (`AnimatedAmount`); (3) sağdaki legend listesi donut dilimleriyle AYNI staggered delay'le (`0.2 + i*0.25`) fade-in-up ile beliriyor, tutarları toplamı tam ₺2.450,00 eden gerçek rakamlar (₺980/₺735/₺367/₺368). Canlı testte animasyonun ORTASINDA (₺1.724,04) ve TAMAMLANDIĞINDA (₺2.450,00) ekran görüntüsü alınarak sayaç ve senkronize legend'ın gerçekten çalıştığı doğrulandı — hem açık hem koyu modda.
- **22 Temmuz 2026 (Faz 6 devamı — 5 bölümdeki "koyu modda takılı kalma" hatası düzeltildi + AI showcase zigzag'a çevrildi):** Kullanıcı canlı testte önceki turlarda 5 bölüme (Kapanış CTA, Yükle/Analiz Et/Rapor Al, Nasıl Çalışır/Step Slider, Planları Karşılaştırın, Footer) bilinçli olarak eklenen `<div className="dark">` sarmalayıcılarının açık modda çalışmadığını, sitenin geri kalanından farklı olarak hep koyu göründüğünü bildirdi. **Kök karar tersine çevrildi:** bu 5 bölümün hepsinden `.dark` sarmalayıcı kaldırıldı, artık sitenin geri kalanıyla aynı şekilde gerçek tema tercihine göre otomatik uyum sağlıyorlar. Kullanıcı örnek olarak literal Tailwind renkleri verdi (`bg-zinc-900`, `text-gray-300` vb.) ama bunlar yerine **projenin token sistemi kullanıldı** (`bg-surface`, `text-text-primary`, `border-border` gibi) — bu token'lar zaten `:root`/`.dark` CSS custom property'leriyle otomatik ışık/koyu geçişi sağlıyor, ayrıca `dark:` varyantı class'ları elle eklemeye hiç gerek kalmadı (proje kuralı: asla hardcoded hex/palet rengi kullanma). En çok iş gerektiren StepSlider'ın "glassmorphism" motoru ekranıydı — sabit `bg-white/5`/`border-white/10`/`text-white/60` referansları `bg-surface/70` (backdrop-blur ile birlikte otomatik açık/koyu cam efekti üretiyor), `border-border`, `text-text-secondary` ile değiştirildi; WorkflowSection'daki nokta-deseni arka plan da sabit `rgba(255,255,255,...)` yerine `color-mix(in srgb, var(--color-text-primary) 6%, transparent)` ile tema-bağımsız hale getirildi. Hem açık hem koyu modda ekran görüntüsüyle (ve `console error` sıfır) doğrulandı — glow/glassmorphism hissi ikisinde de korunuyor, sadece artık gerçekten teması takip ediyor.
- **22 Temmuz 2026:** "Fişştech AI İş Başında" bölümündeki 3'lü yan-yana kart grid'i tamamen kaldırılıp yerine tam genişlikte, zikzak (alternating) düzende 4 adımlık bir "scrollytelling" bölümü kondu — her adım kendi büyük başlığı/açıklaması + özel animasyonlu görseliyle. A11y için DOM sırası her zaman metin-sonra-görsel (screen reader akışı bozulmasın diye), sadece `lg:order-1`/`lg:order-2` ile GÖRSEL pozisyon tek satır atlayarak değişiyor. Adım 3 için yeni bir "doğrulama paneli" animasyonu (bir satır sarıyla işaretli, sonra otomatik tıklanıp yeşil "Onayla" ile onaylanıyormuş hissi veren döngü) sıfırdan tasarlandı. Bu bölüm de yeni inşa edildiği için baştan `.dark` sarmalayıcı OLMADAN, tamamen token-tabanlı yazıldı.
- **22 Temmuz 2026 (Faz 6 devamı — canlı testte bulunan 5 gerçek hata düzeltildi: donut geometrisi, Adım 3 içeriği, Esnaf dinamik fiyat, karşılaştırma tablosu, kamera ikonu):**
  - **Donut grafiği kök neden düzeltmesi:** Önceki turda kullanılan framer-motion `pathLength`/`pathOffset` motion value'ları (elle `strokeDasharray` VERİLMEDEN) çok-segmentli konumlamada güvenilmez çıktı — dilimler tam bir daire oluşturmuyor, sağa sıkışıyordu. Kullanıcının verdiği kesin teknik reçeteyle klasik, öngörülebilir yönteme geçildi: gerçek `circumference = 2πr` (`DONUT_RADIUS`/`DONUT_CIRCUMFERENCE` sabitleri), her segment için `strokeLength = pct * circumference`, statik `strokeDasharray={strokeLength} {circumference}`, statik konumlama CSS `rotate` transform'uyla (`seg.offset * 360`, `transformOrigin: "50% 50%"`), SADECE çizim efekti `strokeDashoffset` (`strokeLength`→`0`) ile animasyonlu. `-90deg` rotasyonuyla saat 12 yönünden başlıyor. Hem açık hem koyu modda ekran görüntüsüyle tam, doğru oranlı bir daire oluştuğu doğrulandı.
  - **Adım 3 içerik ve görsel değişimi ("Son Onay Sizin Elinizde" → "İster İncele, İster Otomatik Kaydet"):** Kullanıcı, sarı-işaretli-satır/Onayla animasyonunun aslında var olmayan bir "manuel kontrol" akışını temsil ettiğini, gerçek bir "Otomatik Fiş Kaydı" özelliğinin vurgulanması gerektiğini belirtti. Eski `ReviewVisual` tamamen kaldırılıp yerine `AutoSaveVisual` kondu: bir ayarlar-UI toggle'ı (`MousePointer2` imleç ikonu toggle'a hareket edip tıklıyormuş gibi, gri→yeşil renk geçişi `animate` ile), ardından bir fiş ikonunun (`Receipt`) hızla bir "Kayıtlı Harcamalar" klasörüne (`FolderCheck`) uçması ve "Arka planda kaydedildi" metninin belirmesi — tek bir 3.4s'lik döngüde, `times` dizileriyle senkronize (dosyadaki mevcut staggered-animasyon deseniyle tutarlı). Artık kullanılmayan `AlertCircle`/`Check` importları kaldırıldı, yerine `Camera`/`FolderCheck`/`MousePointer2`/`Settings` eklendi.
  - **Esnaf Modu kartının büyük fiyatı artık dinamik:** Önceden personel katmanı (Sadece Ben/10/20/40 Kişi) seçilse de kartın üstündeki dev rakam hep sabit 199,99 kalıyordu — kafa karıştırıcıydı. Aylık görünümde büyük rakam artık seçili `selectedTier.monthly`'yi gösteriyor (framer-motion `AnimatePresence`/`motion.span` ile 0.2s fade+slide geçişli), yıllık görünüm ise (katman başına yıllık veri olmadığından) plan'ın taban yıllık fiyatını göstermeye devam ediyor. Buton metni sadeleştirildi — fiyat artık üstte büyük göründüğü için tekrar etmiyor: "Esnaf Modu'na Başla" / "Esnaf (20 Kişi) ile Başla". Canlı tıklama testiyle (10/40 Kişi) hem rakamın hem buton metninin doğru güncellendiği doğrulandı.
  - **Karşılaştırma tablosu düzeltmesi:** "Aylık AI sohbet limiti" satırında Esnaf Modu sütunu yanlışlıkla "500" gösteriyordu — Esnaf Modu işletme/kasa odaklı bir pakettir, AI sohbet özelliği YOK. `esnaf: "500"` → `esnaf: false` (mevcut `Cell` bileşeni `false`'u zaten otomatik soluk `—` olarak render ediyor, component değişikliği gerekmedi).
  - **Adım 1 kamera ikonu düzeltmesi:** Telefon silüetinin merkezindeki ikon yanlışlıkla `Zap` (şimşek) idi, fotoğraf çekme eylemini temsil etmiyordu — `Camera` (`lucide-react`) ile değiştirildi, mevcut konumlandırma/glow/flash-pulse animasyonu aynen korundu.
  - **Doğrulama:** `tsc --noEmit` temiz, `lint` sadece önceden var olan 4 ilgisiz React Hook Form uyarısı (0 hata), `build` başarılı. Playwright ile açık+koyu modda donut/Adım 3 animasyonu (çoklu zaman noktası), Esnaf fiyat geçişi (10/40 Kişi tıklaması), karşılaştırma tablosu ve kamera ikonu ekran görüntüsüyle doğrulandı — `console error` sıfır, 390px'te yatay taşma yok.
- **22 Temmuz 2026 (Faz 6 devamı — pricing kartları seçilebilir + Esnaf personel eklentisi + yeni AI Scanner showcase):**
  - **Karşılaştırma tablosu:** "Çoklu kullanıcı (Grup/Aile)" satırı "Lisans / Kullanıcı Sayısı" olarak, değerleri gerçek lisans mantığını yansıtacak şekilde (Bireysel/1 Kişi+Ek Personel/4 Kişiye Kadar) güncellendi.
  - **Pricing kartları artık tıklanabilir/seçilebilir** — `useState` ile `selectedPlan` (varsayılan: Premium, `plan.popular` alanından türetiliyor), tıklanan kart aktif kalıp yeşil çerçeve/glow'u koruyor, buton stili de seçime bağlı (seçili → dolu yeşil, değilse → outline). `plan.popular` alanı artık SADECE "En Popüler" rozetini kontrol ediyor, çerçeve/glow/buton stilini değil (o iş `isSelected`'a taşındı). Kart `role="button"`/`tabIndex`/`onKeyDown` ile klavye erişilebilir; içindeki gerçek Link ve personel-seçim butonları `stopPropagation` ile kart tıklamasına karışmıyor.
  - **Esnaf Modu kartına personel katmanı seçici eklendi** — 4 seçenek (Sadece Ben/10/20/40 Kişi), gerçek fiyatlarla (bu oturumda daha önce doğrulanmış RevenueCat `esnaf_`/`esnaf10_`/`esnaf20_`/`esnaf40_` paket fiyatları — ₺199,99/349,99/549,99/899,99). Seçime göre "Hemen Başla" butonunun metni dinamik ("Esnaf Satın Al — ₺199,99/ay" / "Esnaf (20 Kişi) Satın Al — ₺549,99/ay") — canlı tıklama testiyle doğrulandı.
  - **Yeni "Fişştech AI İş Başında" bölümü** (Step Slider'ın hemen altına, Step Slider'a HİÇ dokunulmadan) — 3 kart, framer-motion: (1) telefon flaş animasyonu + sürükle-bırak dosya ikonu, (2) fiş üzerinde döngüsel lazer tarama çizgisi + Tarih/Tutar/KDV chip'lerinin senkronize solup parlaması, (3) framer-motion'ın `pathLength`/`pathOffset` motion value'larıyla (SVG `circle` üzerinde, elle `strokeDasharray` VERİLMEDEN — ikisi birlikte çakışıp yanlış render üretiyordu, düzeltildi) sıralı dolan bir donut grafik. **İki gerçek hata canlı derlemede yakalanıp düzeltildi:** (a) `pathOffset` doğrudan JSX prop'u olarak değil `style` içinde verilmeli (TypeScript hatası); (b) kümülatif segment offset'lerini render sırasında bir değişkeni mutasyona uğratarak hesaplamak React Compiler'ın `immutability` kuralına takıldı — düzeltme: modül seviyesinde saf bir `reduce` ile önceden hesaplanmış statik bir dizi.
- **22 Temmuz 2026 (Faz 6 devamı — içerik senkronizasyonu + Raporlar print + app içi navigasyon):** Kapsam bu turda landing page dışına, gerçek uygulamaya (Raporlar, Sidebar, Topbar) da taştı.
  - **Pricing içerik senkronu:** Premium/Esnaf Modu/Aile Planı özellik listeleri kullanıcının verdiği (mobil uygulamadaki güncel) metinlerle birebir değiştirildi. Bulut Yedekleme açıklaması da üçüncü kez güncellendi — artık "sadece orijinal fiş/evrak görseli saklamak isteyen Esnaf Modu kullanıcıları için" gerçek iş kuralını yansıtıyor (önceki metin "Premium/Aile'ye dahil" diyordu, bu YANLIŞTI — düzeltildi).
  - **Footer Bülten:** başlık ile input arasına açıklayıcı bir cümle + `space-y-3` ile orantılı dikey boşluk eklendi.
  - **Yeni "Planları Karşılaştırın" tablosu** (Fiyatlandırma'nın hemen altında, daima koyu sahne) — 9 satırlık karşılaştırma, yeşil tik/tire/metin karışık hücreler, satır hover'ı.
  - **Raporlar sayfası + yazdırma (`window.print`) — kök neden bulundu ve düzeltildi:** Kullanıcının "scrollbar PDF'de çirkin çıkıyor" şikayetinin kaynağı Raporlar'a özel bir div DEĞİL, TÜM uygulamanın paylaştığı `(app)/layout.tsx`'teki `<main overflow-y-auto>` idi — `print:overflow-visible print:h-auto print:block` eklendi, bu düzeltme Raporlar'a özel değil, print kullanan HER sayfaya otomatik yayılıyor. `globals.css`'e scrollbar gizleme + `@page{margin:0mm}` + `body{padding:15mm}` eklendi (tarayıcının varsayılan URL/tarih header-footer'ını kaldırır). **Print rengi:** kullanıcı "Fişştech mavisini andıran" bir print rengi istedi ama Fişştech'in gerçek marka rengi (bu oturumun rebrand kararından beri) YEŞİL — mavi kullanmak tutarsızlık yaratırdı, bunun yerine `--accent` token'ı print başlıklarında kullanıldı (`print:text-accent`). **Ekran temasından bağımsız, her zaman açık/okunabilir print çıktısı** için `:root`'un renk token'ları `@media print` içinde açık mod değerlerine sabitlendi — SADECE `:root`, `.dark` DEĞİL (ilk denemede `.dark`'ı da hedefleyince landing page'in bilinçli-koyu bölümlerindeki (`Footer` vb.) `dark:invert-0` gibi DOM-class'ına bağlı Tailwind varyantlarıyla çakışıp logoyu print'te görünmez yapıyordu — canlı ekran görüntüsüyle yakalanıp düzeltildi, ayrıntı için `globals.css`'teki yorum). Rapor içeriğine **Market Dağılımı** donut grafiği (yeni `expensesByStore()`, `analytics.ts`'in `normalizeStoreName`'ini reuse ediyor) ve Hedefler'e **ilerleme çubuğu** (biriken/hedef tutar) eklendi. Kategori/Market grafiklerinin ikisi de artık recharts'ın kendi Legend'ı yerine özel bir `ReportBreakdownChart` kullanıyor — yüzdelik dilim eklemek ve print'te sol-grafik/sağ-legend hizalamasını garanti etmek recharts Legend içinde çok kırılgan olurdu. Hedefler/Abonelikler/Alınan Ürünler artık print modunda `<table>` yapısına dönüşüyor (`hidden print:table`, açık gri başlık satırı) — ekranda mevcut liste/kart görünümleri (`print:hidden`) korundu.
  - **Uygulama içi navigasyon:** Sidebar/MobileNavDrawer'ın paylaştığı `bottomNavItems`'a (`nav-config.ts`, tek doğruluk kaynağı) Ayarlar'ın hemen altına "Ana Sayfaya Dön" (`/`, Globe ikonu) eklendi. Topbar'ın `UserMenu` dropdown'ına "Profilim" (gerçek bir `/profile` sayfası yok, mevcut `/settings`'e yönlendiriyor — sahte bir sayfa icat edilmedi) ve "Tanıtım Sitesine Dön" (`/`) eklendi. **Logo zaten `/dashboard`'a tıklanabilirdi** (Sidebar + MobileNavDrawer'da) — kullanıcı bunu eksik zannetmiş, kontrol edilip değişiklik gerekmediği doğrulandı.
  - **Doğrulama sınırı:** Raporlar sayfası, Sidebar'ın yeni öğesi ve Topbar dropdown'ı gerçek kimlik doğrulama gerektiriyor — bu ortamda gerçek bir kullanıcı oturumu/test hesabı olmadığından bu üçü tarayıcıda GÖRSEL olarak doğrulanamadı (sadece tsc/lint/build temiz + kod incelemesi). Print CSS mekanizması ise `/login` sayfasında (herkese açık, aynı token sistemini kullanıyor) koyu modda print emülasyonuyla dolaylı olarak doğrulandı — aynı mekanizmanın Raporlar'da da çalışması bekleniyor ama kullanıcının kendi tarayıcısında son bir kontrolü öneriliyor.
- **21 Temmuz 2026 (Faz 6 devamı — pricing interaktivite + Sektörel Çözümler + 3D fiş):** Üç ayrı istek tek turda ele alındı. **(1) Fiyatlandırma kartları:** tüm kartlara (sadece Premium'a değil) hover'da `border-accent` + yeşil glow eklendi (`color-mix` tabanlı arbitrary Tailwind class, popular kartın kalıcı inline glow'uyla çakışmıyor çünkü inline `style` her zaman kazanıyor). Yıllık seçili görünümde artık büyük rakam TOPLAM yıllık tutarı gösteriyor (`/yıl` birimiyle), küçük alt metin aylık karşılığı vurguluyor ("Ayda sadece ₺X'a denk gelir") — önceki tur küçük/büyük tutarları ters göstermişti. Emoji'ler (⭐🏪👨‍👩‍👧‍👦) kaldırılıp `PricingPlan.icon: LucideIcon` alanına geçildi (Crown/Store/Users). Bulut Yedekleme açıklaması kritik iş kuralını (Premium/Aile'ye dahil, sadece Esnaf Modu için ek satın alma) yansıtacak şekilde güncellendi, "Esnaf Modu" alt dizisi component içinde bulunup `<strong>` ile vurgulanıyor (data düz metin kalsın diye). **(2) Yeni "Sektörel Çözümler" bölümü** (Nasıl Çalışır?'den hemen sonra) — 6 sekme, içerik GERÇEK Esnaf Modu alt sistemlerine dayanıyor (Faz 3 Kararlar Günlüğü'nden — uydurma özellik yok): her sekme gerçek alt-sektörler, gerçek açıklama, gerçek özellik listesi + jenerik ama gerçek özellik adlarını gösteren bir "panel" mockup'ı içeriyor. **(3) "Neden Fişştech?"'teki placeholder PNG kaldırıldı**, yerine tamamen CSS/clip-path/framer-motion ile çizilen havada süzülen 3D fiş simülasyonu (`floating-receipt.tsx`) kondu — dış asset'e ihtiyaç kalmadı, `public/receipt-3d-mockup.png` silindi. Fişin kağıt rengi (beyaz) bilinçli olarak site temasından bağımsız tutuldu — gerçek kağıt her zaman açık renktir, marka rengi kararı değil.
- **21 Temmuz 2026:** Canlı testte ikinci bir gerçek mobil-taşma hatası bulundu: Sektörel Çözümler'in mobil sekme çubuğu (`overflow-x-auto`, yatay kaydırma için tasarlanmıştı) yatay kaydırmak yerine SAYFAYI genişletiyordu. Kök neden: CSS Grid item'ları varsayılan olarak `min-width: auto` alır (Flexbox'taki gibi `min-width: 0` değil) — bu da içindeki `overflow-x-auto` kapsayıcının, uzun sekme etiketlerinin ("Yüksek Hacimli Satış" gibi) doğal genişliğine göre grid sütununu (ve dolayısıyla sayfayı) genişletmesine izin veriyordu, kaydırma hiç devreye girmiyordu. Çözüm: ilgili grid item'lara (`Reveal` sarmalayıcılarına) `min-w-0` eklendi — Step Slider'daki dönen-halka taşma hatasından (bir önceki tur) FARKLI bir kök nedene sahip ama aynı kategoriden (CSS'in "içerik kutudan taşabilir" varsayılan davranışları) ikinci bir gerçek hata; her ikisi de sadece statik ekran görüntüsüyle değil, gerçek `scrollWidth` testiyle yakalandı.
- **21 Temmuz 2026 (Faz 6 devamı — özgünlük düzeltmesi + Kapanış CTA yeniden yazımı):** Kullanıcı, örnek verilerdeki "Migros"/"Migros Kampüs"/"migros_fisi.jpg" ibarelerinin referans aldığı sitelere ait placeholder'ları andırdığını, kopyalanmış hissi verdiğini belirtti. Tüm `src/` ağacı "Migros" için taranıp Hero/Step Slider/İş Akışı kartlarındaki 4 occurence "Kampüs Market" / "kampus_market_fisi.jpg" ile değiştirildi. **Bilinçli olarak DOKUNULMAYAN**: `src/lib/data/analytics.ts`'teki `normalizeStoreName()` içindeki "Migros" — bu, gerçek kullanıcıların gerçek fişlerinden OCR ile okunan mağaza adlarını normalize eden GERÇEK bir özellik (mobil'den portlanmış), tasarım placeholder'ı değil; kaldırılması gerçek işlevselliği bozardı. Step Slider'ın dar (11rem) inceleme kartında "Kampüs Market" değeri "Mağaza" etiketiyle yan yana sığmayıp üst üste biniyordu (canlı ekran görüntüsüyle yakalandı) — satır düzeni yatay `justify-between`'den dikey stack'e (`etiket` üstte küçük, `değer` altında `truncate` ile) çevrilerek kalıcı olarak düzeltildi, sadece bu bir string için değil, gelecekte değişebilecek herhangi bir uzun değer için de sağlam. Kapanış CTA'nın tasarımı/metinleri de "referans siteye çok benziyor" gerekçesiyle tamamen yeniden yazıldı: yeni başlık ("Manuel Hesap Devri Kapandı." / "Akıllı Bütçe Dönemi Başlıyor."), sağ-alttan yayılan çok katmanlı radyal gradyan mesh (düz tek renkli kutu yerine), "Hemen Ücretsiz Başla" + ok ikonlu buton, Fişştech'e özgü 3 güven rozeti (Kredi kartı gerekmez / Esnaf ve Aile Modu / Uçtan Uca Şifreleme — jenerik rozetler yerine).
- **21 Temmuz 2026 (Faz 6 — landing page 6 bölümle "premium SaaS" seviyesine taşındı, framer-motion eklendi):** Kullanıcı tek mesajda 5 ayrı, çok detaylı bölüm speсifikasyonu verdi (Step Slider, "Neden Fişştech?", İş Akışı/Analiz kartları, SSS redesign, Kapanış CTA + Footer) ve hepsinde tekrar tekrar `framer-motion` adını açıkça istedi — önceki turda (rebrand sırasında) bilinçli olarak "bağımlılıksız Reveal" tercih edilmişti, ama bu kez kullanıcı kütüphaneyi isimlendirerek İSTEDİĞİ için `npm install framer-motion` ile gerçek bağımlılık olarak eklendi (varsayılanı sessizce koruyup görmezden gelmek yerine).
- **21 Temmuz 2026:** Kullanıcının spesifikasyonlarında sık geçen `text-emerald-500`, `text-gray-400`, `bg-zinc-950`, `border-white/10`, `shadow-green-500/20` gibi ifadeler LİTERAL Tailwind sınıfı olarak değil, "buna benzer bir yeşil/gri" anlamında örnek/referans olarak yorumlandı — hepsi projenin token sistemine (`text-accent`, `text-text-secondary`, `bg-surface`, `border-border`, `color-mix(in srgb, var(--color-accent) ...)`) çevrildi. Kullanıcının kendi metninde de zaten "örneklerdeki tonları KULLANMA, bizim paletimizi kullan" diye açıkça yazması bu yorumu doğruluyor; ayrıca önceki pricing-redesign turunda `text-gray-400` önerisi aynı şekilde `text-text-secondary`'ye çevrilmiş ve kullanıcı buna itiraz etmemişti.
- **21 Temmuz 2026:** Spesifikasyonlarda tekrar eden "koyu/dark mode zemin kullan" ifadesi bölüme göre farklı yorumlandı, kullanıcıya açıkça belirtildi: Step Slider, İş Akışı/Analiz kartları, Kapanış CTA ve Footer — glow/orbital/glassmorphism efektlerinin okunaklı olması için kasıtlı olarak DAİMA koyu (`<div className="dark">` sarmalayıcıyla, site temasından bağımsız — birçok premium SaaS sitesinde "vitrin" bölümleri böyle çalışır, örn. Linear'ın ürün demo bölümü). "Neden Fişştech?" ve SSS ("Merak Edilenler") ise sayfanın geri kalanıyla aynı şekilde TEMA-UYUMLU bırakıldı (aksi halde açık moddayken sayfanın ortasında rastgele bir koyu ada gibi dururdu). `.dark` class'ının bir alt-ağaca sarmalayıcı olarak uygulanması, `tokens.css`'teki `.dark { --bg: ...; }` seçicisinin CSS custom property cascade'i sayesinde otomatik çalışıyor — hardcoded hex'e hiç gerek kalmadı.
- **21 Temmuz 2026:** "Neden Fişştech?" bölümündeki görsel için kullanıcı `public/receipt-3d-mockup.png` yolunu verip "ben sonra ekleyeceğim" dedi — ama dosya henüz yoktu, next/image bu haliyle 404 verip görsel alanının kırık görünmesine yol açacaktı. Playwright ile (yine hiçbir yeni image-processing bağımlılığı eklemeden) markaya uygun, "3D fiş görseli buraya eklenecek" yazılı bir yer tutucu PNG üretilip TAM O DOSYA ADIYLA `public/`'a kondu — kullanıcı gerçek 3D görseli eklediğinde tek yapması gereken aynı dosyayı üzerine yazmak, kod tarafında hiçbir değişiklik gerekmiyor.
- **21 Temmuz 2026:** Footer'da istenen "Visa/Mastercard/Troy/Stripe/iyzico" ödeme logoları BİLİNÇLİ OLARAK eklenmedi — hem gerçek marka SVG dosyaları elde yok (uydurma/yanlış vektör çizmek riskli) hem de daha önemlisi, sitede gerçek bir ödeme altyapısı YOK (satın alma App Store/Play Store üzerinden) — kartlı ödeme ağı logoları göstermek, sitenin doğrudan kart ödemesi aldığı izlenimini verip yanıltıcı bir güven sinyali olurdu. Bunun yerine dürüst bir "Güvenli Ödeme" notu + App Store/Google Play'in kendi altyapısını kullandığını açıklayan bir cümle kondu. Aynı gerekçeyle sosyal medya ikonları (X/Instagram/LinkedIn) gerçek URL'si bilinmediği için TIKLANABİLİR LİNK değil, dekoratif (title="...yakında") olarak bırakıldı — proje kuralı "asla URL uydurma"ya uyularak.
- **21 Temmuz 2026:** `lucide-react`'ın kurulu sürümünde (1.25) TÜM marka/sosyal ikonları (Instagram/Linkedin/Twitter dahil) kaldırılmış olduğu keşfedildi (`node_modules` içinde doğrudan doğrulandı) — bu yüzden Footer'ın sosyal ikonları için aynı stroke stiliyle (currentColor, 24x24) 3 adet sade/jenerik inline SVG yazıldı, gerçek logoların birebir taklidi değil.
- **21 Temmuz 2026:** Footer'daki "Yardım Merkezi/Veri Güvenliği/Gizlilik/Kullanım Şartları/KVKK/Uygulamayı İndir" linkleri için içerik henüz yazılmadığından, projenin "asla ölü link olmaz" kuralına uyularak 6 yeni gerçek rota (`/yardim`, `/veri-guvenligi`, `/gizlilik`, `/kullanim-sartlari`, `/kvkk`, `/indir`) + paylaşılan bir `LegalStub` bileşeni oluşturuldu (uygulama içi `ComingSoonPage`'den farklı — o bileşenin metni "mobilde kullanmaya devam edebilirsiniz" diyor ki bu yasal/destek sayfaları için uygun değil). Bu 6 rota `src/lib/supabase/middleware.ts`'teki `PUBLIC_ROUTES`'a eklendi — aksi halde anonim bir Footer ziyaretçisi bu linklere tıklayınca `/login`'e atılırdı.
- **21 Temmuz 2026:** Bülten (newsletter) formu için gerçek bir e-posta pazarlama altyapısı yok — kayıt sahte bir "başarılı!" mesajıyla sessizce hiçbir yere kaydedilmeden "çalışıyormuş gibi" gösterilmedi (AGENTS.md kural 4). Bunun yerine projenin zaten kullandığı "yakında" deseniyle (bkz. Hızlı Fatura PDF, Ayarlar bildirimleri) dürüst bir `window.alert` bilgilendirmesi kondu.
- **21 Temmuz 2026 (canlı testte bulunan gerçek hata):** Playwright ile 390px genişlikte tam sayfa yatay-taşma testi çalıştırıldı, `document.documentElement.scrollWidth > clientWidth` TRUE döndü. Kök neden: Step Slider'daki dönen yörünge halkası `border-radius: 9999px` (`rounded-full`) ile görsel olarak DAİRE ama element'in kendi kutusu hâlâ KARE (320×320) — CSS `rotate()` transform'u bu kareyi döndürüyor, ve döndürülmüş bir karenin eksene-hizalı bounding box'ı (`getBoundingClientRect`) döndürülmemiş halinden BÜYÜK oluyor (45°'de 320×√2≈452px'e kadar) — dairenin kendisi rotasyonel simetrik olduğu için görsel olarak hiçbir şey değişmiyor ama element'in ölçülen kutusu büyüyor ve dar ekranlarda sayfanın `scrollWidth`'ini artırıyor. Çözüm: halka konteynerine `overflow-hidden` eklendi (döndürülmüş kutunun görünmeyen "köşe" taşmasını kırpıyor, daire zaten kutunun tam sınırında olduğu için hiçbir görsel kesilme YOK, sadece halka üzerindeki küçük parlayan noktanın rotasyonun bazı anlarında ~birkaç piksel kırpılması gibi ihmal edilebilir bir yan etki var). Düzeltme sonrası `scrollWidth` testi tekrar çalıştırılıp `false` (taşma yok) doğrulandı.
- **18 Temmuz 2026:** Web sitesinde tema seçimi 4'ten (mobildeki Yeşil/Karanlık/Vintage/Gül) 2'ye (Açık/Koyu) düşürüldü. Gerekçe: web'de öncelik marka tutarlılığı ve profesyonel ilk izlenim.
- **18 Temmuz 2026:** Tech stack: Next.js (App Router, TypeScript) + Tailwind CSS v4 + Supabase (`@supabase/supabase-js` + `@supabase/ssr`). Next.js 16 kurulduğu için `middleware.ts` yerine yeni `proxy.ts` dosya kuralı kullanıldı (kırıcı değişiklik — paketin kendi `node_modules/next/dist/docs/` dokümantasyonundan doğrulandı).
- **18 Temmuz 2026:** Açık Mod token'ları: bg `#F8F5F2`, surface `#FFFFFF`, accent `#B23A65` (mobil CTA rengiyle aynı), text-primary `#241A1E`, text-secondary `#6B5A60`. Koyu Mod: bg `#1B1417`, surface `#241B1F`, accent `#E85D8A` (AA kontrastı için parlatıldı), text-primary `#F5EDEF`. Semantik renkler (success/danger) koyu modda mobil değerlerinden açıklaştırıldı çünkü ham değerler koyu zeminde AA kontrastını geçemiyordu (hesaplanan kontrast oranları koda yorum olarak not düşüldü).
- **18 Temmuz 2026:** Font çifti: Bricolage Grotesque (display) + Plus Jakarta Sans (body), `next/font/google` ile self-host.
- **18 Temmuz 2026:** Navigasyon: sol sidebar (masaüstünde tam, tablette ikon-rail, mobilde slide-over drawer) — 15 üst düzey bölüm bir top-nav'a sığmadığı için.
- **18 Temmuz 2026:** `src/lib/types/database.ts` el yazımı olarak, `fisle_app/supabase/migrations/*.sql` dosyaları doğrudan okunarak (tahmin değil) oluşturuldu. `supabase login` sonrası `supabase gen types typescript` ile değiştirilmesi planlanıyor.
- **18 Temmuz 2026:** Fiş Tara kredi mantığı mobildeki `scan_service.dart`/`scan_screen.dart` davranışıyla birebir: kredi taramadan ÖNCE tüketiliyor; tarama başarısız/okunamaz olsa bile kredi otomatik iade EDİLMİYOR (sadece kullanıcı inceleme ekranında "İptal" derse iade ediliyor). Uzun Fiş (çoklu) modunda tüm sayfalar için TEK kredi harcanıyor, sayfa başına değil.
- **18 Temmuz 2026:** Gerçek kullanıcı hesabıyla canlı testte bulunan hata düzeltildi — `Sidebar` bir Server Component olarak Lucide ikon bileşeni içeren `NavItem` objesini `"use client"` olan `NavLink`'e prop olarak geçiriyordu; React Server Components fonksiyon/bileşen referanslarının server→client sınırını geçmesine izin vermiyor ("Only plain objects can be passed to Client Components..."). Çözüm: `Sidebar`'a `"use client"` eklendi (zaten sunucu tarafı bir işi yoktu). **Genel kural:** bir Server Component, içinde bileşen/fonksiyon referansı barındıran bir objeyi (örn. `{icon: LucideIcon}`) bir Client Component'e prop olarak geçiremez — ya kaynak Server Component'i client yap, ya da ikonu orada `<item.icon />` olarak elemente çevirip children/slot olarak geçir.
- **18 Temmuz 2026:** Dashboard, gerçek kullanıcı hesabıyla (mevcut mobil verileriyle) canlı doğrulandı — ay seçici, aylık toplam hero kart, Gelir/Gider/Net Bakiye, boş grup durumu doğru render oluyor.
- **18 Temmuz 2026:** Faz 2 kararı: kullanıcı "faz 2 başla hepsini yap" dedi — tüm bireysel modüller tek oturumda, aynı desenle (Server Component veri çekimi + Client Component dialog form + `supabase` browser client ile doğrudan insert/update/delete + `router.refresh()`) inşa edildi. Grafikler için `recharts` eklendi (Tailwind CSS custom property'leriyle temaya uyumlu).
- **18 Temmuz 2026:** AI Sohbet için mobil kod tabanında zaten var olan `supabase/functions/fisle-ai-chat/index.ts` keşfedildi — tam sunucu taraflı, JWT doğrulamalı, kullanıcının harcama verisini kendi çekip Gemini'ye bağlam olarak veren, `{reply}` döndüren bir edge function. Web `scan-receipt` ile aynı desenle (`supabase.functions.invoke`) buna bağlandı — yeni bir sunucu mimarisi kurmaya gerek kalmadı.
- **18 Temmuz 2026:** Gruplarım kasıtlı olarak temel tutuldu. `004_fix_rls_recursion.sql`'deki `group_invites_manage` politikası `FOR ALL USING (group_id IN get_owned_groups())` — yani grup SAHİBİ olmayan bir kullanıcı, kendisine gönderilen bir daveti token ile SELECT bile edemiyor (RLS engelliyor). Mobil kodda `getInviteByToken()` düz bir `.select()` çağrısı yapıyor; bu policy ile bu çağrının normal (owner olmayan) bir davetli için çalışması teknik olarak mümkün görünmüyor — ileride bu ya yeni bir SECURITY DEFINER RPC (bkz. `accept_business_invite` örneği) ile çözülmeli ya da mobildeki gerçek davranış daha derinlemesine doğrulanmalı. Bu nedenle web'de davet/katılma akışı bilinçli olarak ERTELENDİ, yanlış çalışan bir şey inşa edilmedi.
- **18 Temmuz 2026 (canlı testte bulunan 3 hata düzeltildi):**
  1. `NavLink` bileşeninde `collapsed && "lg:hidden"` yazılmıştı — Tailwind'de bu, etiketi TAM TERSİ şekilde masaüstünde (lg+) gizliyordu, tablette gösteriyordu. Doğrusu `collapsed && "hidden lg:inline"` — masaüstünde göster, tablette (icon-rail) gizle. `Sidebar`'daki `md:w-18` de geçersiz bir Tailwind class'ıydı (Tailwind'in standart skalasında "18" yok) → `md:w-16` yapıldı.
  2. AI Sohbet hata veriyordu ama neden belirsizdi — `invokeAiChat` hata mesajını görmezden gelip jenerik bir mesaj gösteriyordu. `@supabase/functions-js`'in `FunctionsHttpError.context`'i (ham `Response` objesi) okunarak edge function'ın gerçek `{error: "..."}` gövdesi kullanıcıya gösterilecek şekilde düzeltildi — asıl sebep bir sonraki canlı testte görülecek.
  3. **"Özet Oluştur" yanlış anlaşılmıştı** — AI Sohbet'e yönlendirilmişti ama mobildeki gerçek karşılığı tamamen ayrı bir modül (`features/reports/`): dönem seçimi + `ReportService.generateReport()` ile PDF/Excel'e dönüştürülen kapsamlı bir finansal rapor. `report_service.dart` ve `report_generator.dart` incelenerek doğru şekilde `/reports` altında yeniden inşa edildi. Ayrıca "Alınan Ürünler" tablosu, kullanıcı geri bildirimiyle mobildeki gibi tarih bazlı çerçeveli gruplara dönüştürüldü.
- **18 Temmuz 2026:** `fisle-ai-chat` edge function'ında `GEMINI_MODELS = ['gemini-1.5-flash']` — bu model Google tarafında artık mevcut değil (404). Kullanıcıya düzeltilmiş kod verildi (`['gemini-2.5-flash', 'gemini-2.0-flash']`, `scan-receipt`'in zaten kullandığı modelle aynı) — kullanıcı Supabase Dashboard'dan deploy etti. Model hatası düzeldi ama ardından Gemini API 429 (kota) hatası çıktı — bu artık kod sorunu değil, Google AI Studio/Cloud tarafındaki key'in kota/faturalandırma kısıtı. Kullanıcı kararıyla bu konu ERTELENDİ, Faz 3'e geçildi.
- **18 Temmuz 2026:** Faz 3 (Esnaf Modu) kapsamı: kullanıcı "diğer faza geç" dedi, mobildeki gerçek build sırası (önce ortak altyapı — `026_esnaf_modu.sql`, sonra sektörel modüller) esas alınarak SADECE ortak altyapı bu oturumda inşa edildi. 6 sektörün her biri mobilde ayrı, büyük alt sistemler (kendi tabloları, kendi ekran setleri) olduğu için tek oturumda hepsini yapmak yerine önce sağlam bir temel kuruldu; sektör modülleri sıradaki iş.
- **18 Temmuz 2026:** Esnaf Modu'nda aktif işletme takibi için mobildeki `SharedPreferences` yaklaşımı yerine web'e uygun bir cookie + Next.js Server Action deseni kullanıldı (`src/lib/esnaf/active-business.ts`) — Server Component'lerin de aktif işletmeyi okuyabilmesi için (client-only state yeterli olmazdı).
- **18 Temmuz 2026:** Esnaf Modu premium gating'i (mobildeki `esnaf_plan === 'esnaf_premium'` kontrolü) web'e kasıtlı olarak taşınmadı — web'de henüz ödeme/premium satın alma akışı yok, gating eklemek özelliği test edilemez hale getirirdi. Bkz. Açık Sorular.
- **18 Temmuz 2026:** Kullanıcı mobil ekran görüntüsüne bakarak 3 somut ekleme istedi: Kasa Defteri'ne "Bugünkü Ciro" kartı, Yeme-İçme'ye özel Menü Yönetimi, tüm sektörlere Evrak Arşivi + seri tarama. Menü Yönetimi için `031_kafe_restoran.sql`'deki `menu_categories`/`menu_items` şeması doğrudan okunarak doğrulandı. Evrak Arşivi için ayrı bir "documents" tablosu OLMADIĞI için (mobil şemada yok), `invoices.image_url` alanı üzerinden modellendi — taranan her evrak bir fatura kaydı oluşturuyor.
- **18 Temmuz 2026:** Evrak tarama için YENİ bir edge function kurmak yerine mevcut `scan-receipt` (Fiş Tara'nın kullandığı, zaten deploy ve test edilmiş) yeniden kullanıldı — AGENTS.md kural 7 gereği (Esnaf OCR client-side key ile YAPILMAZ), yeni bir sunucu fonksiyonu gerekirdi ama bu iş mevcut altyapıyla (farklı bir alan eşlemesiyle: store_name→karşı taraf, total→tutar) çözülebildiği için tercih edildi. Kesin doğruluk mağaza fişi kadar yüksek olmayabilir (prompt fiş odaklı) — kullanıcı sonucu her zaman düzenleyip onaylıyor, bu yüzden güvenli.
- **18 Temmuz 2026:** `.next` klasörünü çalışan dev server ile aynı anda silmek "Internal Server Error" (dev server'ın normal hata ekranı bile açamayacağı bir çökme) yarattı — bunu iki kez yaşadık. Kural: dev server çalışırken `.next` silinecekse önce sunucu durdurulmalı (port 3000'i dinleyen process kill edilip `.next` silinip sunucu yeniden başlatılmalı), asla canlı sunucunun altından silinmemeli.
- **18 Temmuz 2026 (mobil detay-parite geçişi):** Kullanıcı "web, mobil kadar detaylı değil" geri bildirimiyle Harcamalarım/Gelir-Gider/Borçlarım ekranlarının fisleapp'teki (`manual_expense_screen.dart`, `income_fixed_expense_screen.dart`, `debt_bottom_sheet.dart`, `period_helper.dart`, `category_predictor.dart`, `export_service.dart`) TAM özellik setinin çıkarılıp uygulanmasını istedi. Read-only bir keşif ajanıyla mobil kaynak dosyaları satır satır incelendi, sonra web'e birebir portlandı: ortak `PeriodHelper`/`PeriodSelector` (`src/lib/utils/period.ts`, `src/components/ui/period-selector.tsx`), 250 kurallık kategori tahmin sözlüğü (`src/lib/expenses/category-predictor.ts`), taksit hesap mantığı (`src/lib/expenses/installment.ts`), Excel export (`src/lib/income/export.ts`, `xlsx` paketi eklendi). Gelir-Gider ekranındaki NET BAKİYE formülü mobilde ekran/Excel arasında tutarsızdı (ekran: Gelir−Sabit, Excel: Gelir−Sabit−Değişken) — web'de bilinçli olarak Excel'deki (daha doğru, 3 bileşenli) formül tek doğruluk kaynağı yapıldı, mobildeki hata tekrarlanmadı.
- **18 Temmuz 2026:** `xlsx` (SheetJS Community Edition) npm paketinin güncel sürümünde bilinen Prototype Pollution / ReDoS güvenlik uyarıları var — ancak bunlar `XLSX.read()` ile GÜVENİLMEYEN dosya AYRIŞTIRIRKEN tetiklenir. Web'de sadece `XLSX.write`/`writeFile` (dosya ÜRETME) kullanılıyor, hiçbir yerde kullanıcı tarafından yüklenen bir xlsx dosyası parse edilmiyor — bu risk yüzeyinin dışında, bilinçli kabul edildi.
- **18 Temmuz 2026:** Tasarım sistemi kuralı hatırlatması: `warning` (#F5A623) rengi bg üzerinde sadece 1.87:1 kontrast veriyor — düz metin rengi olarak KULLANILAMAZ (sadece dolgu rozet/ikon). Değişken Giderler sekmesinde harcama tutarları bu yüzden `text-warning` değil `text-accent` ile renklendirildi.
- **18 Temmuz 2026 (ikinci detay-parite turu — kullanıcı canlı testte 4 somut sorun bildirdi):**
  1. **`Switch` bileşeninde hizalama hatası** ("taksit butonu açınca yamuluyor", "hatırlatıcıdaki buton kayıyor") — kök neden: thumb `<span>`'ına `left` değeri hiç verilmemişti (`absolute top-0.5 ...` ama `left-*` yok), bu da tarayıcıya göre farklı statik konumlanmaya (drift) yol açıyordu. `left-0.5` sabit taban + `checked` olunca `translate-x-5` deltası şeklinde düzeltildi — artık her yerde (taksit toggle'ı, hatırlatıcı toggle'ı, gruba-ekle görünürlük toggle'ı) tutarlı.
  2. **"Taksitler uygulanmıyor" — asıl eksik, mobildeki "Ödeme Takvimi" ekranıydı.** Web'de taksitli bir harcamaya tıklayınca sadece düzenleme formu açılıyordu; mobilde ayrı bir detay sayfası (`expense_detail_screen.dart`) taksit taksit "Ödendi/Bekliyor" listesi, ilerleme çubuğu, düzenlenebilir Başlangıç Tarihi (değişince TÜM ödeme tarihleri + paid-state'ler yeniden hesaplanıyor — mobildeki gerçek davranış, elle işaretlenmiş özel durumlar bu işlemde kasıtlı olarak sıfırlanıyor) ve salt-okunur Bitiş Tarihi gösteriyordu. Bu birebir portlandı: `/expenses/[id]` (yeni route) + `InstallmentScheduleCard` (`src/components/modules/expenses/installment-schedule-card.tsx`) + taksit hesap yardımcıları (`src/lib/expenses/installment.ts`: `paymentDate`, `autoPaidStates`, `autoAdvance`, `remainingAmount`). Her taksit satırına tıklayınca `installment_plans.paid_states` + türetilmiş `paid_count` birlikte güncelleniyor (mobildeki `_persist` ile aynı). Harcamalarım listesinde taksitli harcamalar artık bu detay sayfasına, normal harcamalar hâlâ doğrudan düzenleme formuna gidiyor.
  3. **Analiz sayfası mobil kadar detaylı değildi.** `analytics_screen.dart` (2967 satır) satır satır incelenip 4 sekmeli yapı birebir portlandı: **Harcamalar** (dönem filtresi + kaydırmalı/dot-indicator'lı carousel: Kategori Dağılımı donut / Genel Kategoriler donut (parent_group bazlı, mobildeki `parentGroupIcon()` switch'i birebir portlandı) / Market Analizi (mağaza adı normalize edilip donut + "Alışveriş Sıklığı" bar chart) + "6 Aylık Trend"), **Gelir-Gider** (ayrı "Gelir-Gider Karşılaştırması" bar chart + ayrı "6 Aylık Net Bakiye Trendi" line chart, sıfır çizgisi + işarete göre yeşil/kırmızı nokta rengi — mobildeki gibi TEK combined chart değil, İKİ AYRI kart), **Abonelikler** (özet kart, "Bu yıl toplam X harcandı" bilgi kutusu, Aylık Maliyet Trendi, Zaman Çizelgesi/Gantt timeline, "Tüm Abonelikleri Yönet"), **Grup** (grup seçici + dönem filtresi + Üye Bazlı Harcama progress bar listesi, `getMemberSpendTotals()` yeni eklendi). Kategori kırılımı hesaplaması mobildeki gibi taksitli harcamaları "sadece dönem içine düşen VE ödenmiş taksit tutarı" olarak, aktif abonelikleri de dönem uzunluğuna göre ölçeklenmiş ("Abonelikler" adında) bir sözde-kategori olarak katıyor (`_installmentBoundedAmount`/`_subMultiplier` birebir port edildi). **Not:** Genel Kategoriler sekmesinde taksitli harcamaların parent_group ataması, mobil kaynağın bu kısmı raporda kesilmiş olduğu için item bazlı mantıktan simetrik olarak çıkarsandı (doğrudan doğrulanmadı) — bkz. Açık Sorular. Bütçe/Limit kartı (`category_limits` tablosu) bu turda kapsam dışı bırakıldı, web'de henüz limit CRUD'u yok.
  4. **6 aylık grafiklerin çizgisi başlangıç değerinden geçmiyordu.** Kök neden muhtemelen Bar+Line'ın tek bir `ComposedChart`'ta karışık kullanılmasıydı. Çözüm olarak mobildeki gibi AYRI kartlara bölündü + trend çizgileri için Y ekseni sıfırdan başlayacak şekilde sabitlendi (`domain={[0,...]}`) + çizginin altına hafif gradient dolgu (`Area`) eklendi — mobildeki "alan altı gradient dolgu" detayına daha yakın.
  5a. **"Kaydırarak geçme" mobil deseni web'e uygun değildi.** Kullanıcı: "bir web sitesi geliştiriyoruz mobil uygulama değil, kaydırarak geçmeli şeyler olmamalı" — Harcamalar sekmesindeki `ChartCarousel`'in dot-indicator + touch-swipe mantığı tamamen kaldırıldı, yerine sitenin geri kalanıyla tutarlı sıradan `Tabs` (tıklanabilir sekme) bileşeni kondu. Genel ilke: bundan sonra hiçbir yeni bileşen swipe/touch tabanlı sayfalama kullanmayacak.
  5b. **"Son 6 Ay Trend" (bar+line combo) grafiğinde sıfır çizgisi eksikti.** `/balance` (Net Bakiye & Gelir-Gider), Esnaf Raporları ve Özet Oluştur'un paylaştığı `BalanceTrendChart`'a (ComposedChart) `ReferenceLine y={0}` eklendi — Analiz sayfasındaki yeni `NetBalanceTrendChart`'ta zaten vardı, bu eski/paylaşılan bileşende eksikti.
  6. **Abonelik formu/listesi mobil kadar detaylı değildi.** `add_subscription_screen.dart` incelenip form genişletildi: Periyot toggle, Başlangıç Tarihi (isteğe bağlı), Bitiş Tarihi (isteğe bağlı), Ödeme Yöntemi (Belirtme/Kredi Kartı/Banka Kartı/Nakit + kart etiketi), "Hatırlatmaları Aç" toggle (mobildeki "Yenileme tarihinden 3 gün önce" alt yazısıyla — **not:** bu sadece veri olarak kaydediliyor, web'de henüz gerçek bir bildirim gönderilmiyor, Borçlarım'daki Hatırlatıcı ile aynı sınırlama), Durum dropdown, canlı Aylık/Yıllık dönüşüm önizlemesi. **Önemli mobil-doğru detay:** Sonraki Yenileme Tarihi başlangıç tarihinden OTOMATİK hesaplanmıyor (mobilde de öyle) — kullanıcı elle giriyor. Liste artık satıra tıklayınca düzenleme modunu da destekliyor (öncesinde sadece ekle/duraklat/sil vardı).
- **19 Temmuz 2026:** Mobil detay-parite turu kullanıcı tarafından onaylandı, iki ayrı commit atıldı (Esnaf Modu ortak altyapısı Faz 3 + bu turdaki tüm detay-parite işi — önceki oturumlardan beri hiç commit edilmemişti, ikisi de aynı anda temizlendi). Ardından kullanıcı "diğer faza geç" dedi — `AskUserQuestion` ile hangi faz olduğu netleştirildi: Esnaf Modu'nun kalan 5 sektörel modülü. Önce bir Explore ajanıyla mobildeki `lib/features/esnaf/` klasörünün TAMAMI (6 sektör × ekranlar × Supabase tabloları) satır satır çıkarıldı — sektörler arası paylaşım/bağımsızlık haritası netleşti (bkz. rapor: her sektör tamamen ayrı tablo+ekran seti, sadece Hızlı Perakende'nin POS'u diğer panolardan kısayol olarak çağrılıyor). Rapor kendi içinde en basit/en temel modül olarak **Hızlı Perakende**'yi önerdi (diğer sektörlerin de "hızlı nakit girişi" için kullandığı alt sistem) — bu sırayla başlanmasına karar verildi.
- **19 Temmuz 2026:** Hızlı Perakende modülü (`029_perakende_modulu.sql`'deki 7 tablo — migration zaten production'da GRANT+RLS ile mevcut, yeni migration gerekmedi) web'e taşındı: **Ürünler** (kategori + ürün CRUD, kategori pill filtreleri), **Hızlı Kasa/POS** (ürün ızgarası → sepet, miktar +/-, Nakit/Kart/Veresiye ödeme, veresiye seçilince müşteri arama+hızlı-ekle picker'ı açılıyor), **Veresiye Defteri** (müşteri listesi + açık bakiye, tahsilat alma, hareket geçmişi). `EsnafSubNav`'a sektöre koşullu 3 ek sekme eklendi (kafe'nin Menü sekmesiyle aynı desen). Kasıtlı olarak bu turda ERTELENEN mobil özellikler (kod referansı YOK, hepsi placeholder/basitleştirme): PLU kod ile otomatik sepete ekleme, ürün varyasyonları, gün sonu toplu fiş tarama (Evrak Arşivi'ndeki seri-tarama deseniyle sonra eklenebilir), Perakende Giderleri ve Satış Geçmişi ekranları (route yok), Ayarlar. `sale_count` artışı gerçek bir DB-seviyesi atomik RPC yerine oku-yaz deseniyle yapılıyor (tek kullanıcılı POS için yeterli, mobilde de eşdeğer bir garanti yok).
- **19 Temmuz 2026:** Kullanıcı Hızlı Perakende'yi doğrulamadan "faz 3'e başla" + "faz 4'e başla" dedi (art arda, tek mesajda iki talimat). Bir önceki turda zaten "Faz 3 = kalan 5 esnaf sektörü" netleşmişti, bu yüzden ek soru sormadan devam edildi: Faz 3 için sıradaki sektör olan **Hizmet & Bakım**'a başlandı (`030_hizmet_modulu.sql`'deki 6 tablo — migration zaten production'da mevcut). Taşınanlar: **Müşteriler** (ad/telefon/plaka/cihaz modeli CRUD), **Hizmet Kataloğu** (ad/fiyat/süre CRUD), **Ajanda** (08:00-21:30 arası 28×30dk sabit slot günlük görünüm, boş slota tıklayınca sihirbaz açılıyor), **Atölye** (Bekliyor/Devam/Hazır 3 sütunlu Kanban, ileri-akış butonlarıyla durum değiştirme), **Hizmet Sihirbazı** (3 adımlı ortak dialog — müşteri seç/hızlı-ekle/müşterisiz devam et → katalogdan hizmet + dinamik parça satırları → personel + ödeme yöntemi + not — `preStartTime` verilirse `appointments`'a, verilmezse doğrudan `service_jobs`'a yazıyor, ikisi de aynı sihirbazı kullanıyor). **WhatsApp entegrasyonu dahil edildi** (mobildeki `whatsapp://send` yerine web-uyumlu `https://wa.me/...` deep-link — backend gerektirmediği için, Borçlarım/Abonelikler'deki push bildirimi gibi ERTELENMEDİ, iş "Tamamla" durumuna alınınca müşteri telefonu varsa bir WhatsApp gönderme kartı çıkıyor). Kasıtlı olarak ERTELENEN: sektöre özel Pano (mobildeki "Akıllı Bildirimler" bakım hatırlatmaları + usta primleri) — genel `/esnaf/pano` şimdilik değişmedi.
- **19 Temmuz 2026:** Faz 4 (Senkronizasyon) için "gerçek zamanlı senkronizasyonu nereye uygulayalım?" sorusu `AskUserQuestion` ile soruldu, kullanıcı **"Tüm uygulama"**'yı seçti. Önce mobil tarafın Realtime kullanım deseni doğrulandı: mobil `supabase_realtime` publication'ına SADECE `group_messages`, `expense_groups`, ve kafe-staff senkronu için `restaurant_tables`/`restaurant_orders`/`order_items`/`menu_items`'ı eklemiş — geri kalan ~30 tablo (expenses, incomes, business_incomes, quick_products, appointments, service_jobs, vb.) publication'da YOK, yani bunlara `postgres_changes` ile abone olmak (publication'a eklenmeden) sessizce hiç olay almaz — bu netleşmeden kod yazılsaydı çalışmayan/sahte bir özellik olurdu. Bunun yerine: (1) `docs/sql/047_web_realtime.sql` yazıldı — kullanıcının Supabase Dashboard'dan çalıştırması gereken, eksik ~30 tabloyu publication'a ekleyen migration (REPLICA IDENTITY FULL'a gerek yok çünkü web sadece "bir şey değişti" sinyaliyle `router.refresh()` tetikliyor, eski/yeni satır farkını kullanmıyor); (2) tek bir yeniden kullanılabilir `useRealtimeRefresh(tables)` hook'u + `<RealtimeRefresh />` wrapper'ı yazıldı, `(app)/layout.tsx` (11 kişisel tablo) ve `esnaf/(dashboard)/layout.tsx`'e (21 esnaf tablosu) birer satırla eklendi — her sayfaya ayrı ayrı abonelik kodu yazmak yerine layout seviyesinde tek noktadan tüm alt sayfalara yayılıyor. RLS zaten Realtime olaylarını da süzdüğü için (kullanıcı sadece SELECT edebildiği satırların değişikliklerini görür) client tarafında `user_id`/`business_id` filtresine gerek kalmadı — bu da tasarımı önemli ölçüde basitleştirdi.
- **19 Temmuz 2026:** Kullanıcı "faz 3 tamamlandıysa faz 4'e geç, tamamlanmadıysa tamamla" kuralını koydu — Faz 3 hâlâ tamamlanmamıştı (6 sektörden 3'ü bitmişti), bu yüzden Faz 4 zaten kod tarafı bitmiş olsa da bir kenara bırakılıp Faz 3'e devam edildi. Sıradaki (araştırma raporunun karmaşıklık sıralamasına göre) **Toptancı & İmalatçı** sektörüne başlandı (`032_toptan_imalat.sql`'deki 6 tablo — migration zaten production'da mevcut). Taşınanlar: **Depo & Stok** (`inventory` — CRUD + kritik seviye rozeti + hızlı +/- stok düzeltme), **Bayiler** (`b2b_customers` — risk seviyesi/kredi limiti CRUD, cari bakiye + limit-aşımı rozeti, cari hareket geçmişi + Nakit/Havale/Çek tahsilat kaydı — çek/havale referans no + çek vadesi alanlarıyla), **Toplu Sipariş** (`wholesale_orders`+`wholesale_order_items` — bayi seç, depo ürünlerinden sepete ekle, satır bazlı %iskonto), **Sevkiyat** (Bekliyor/Hazırlanıyor/Yolda 3 sütunlu Kanban — mobildeki gibi "Teslim Edildi" onayında sipariş tutarı kadar otomatik `b2b_transactions` borç kaydı + `b2b_customers.current_debt` güncellemesi, bu davranış rapor tarafından doğrulanmıştı). **Bilinçli olarak DOKUNULMAYAN bir mobil hata**: mobilde Toptan panosundaki "Cari Hesap" kısayolu yanlışlıkla Perakende'nin Veresiye Defteri route'una gidiyordu (b2b_customers/perakende_customers model uyuşmazlığı) — web'de bu kopyalanmadı, doğru B2B cari ekranı (Bayi Detay dialogu) baştan doğru inşa edildi. Yeni 6 tablo hem `esnaf/(dashboard)/layout.tsx`'teki Realtime listesine hem `047_web_realtime.sql`'e eklendi (Faz 4 kod tarafı zaten var, yeni sektör tabloları da otomatik kapsama alındı).
- **19 Temmuz 2026:** Kullanıcı "devam et" dedi, Toptancı & İmalatçı commit edilip sıradaki (araştırma raporunun karmaşıklık sıralamasına göre en zoru Yüksek Hacimli Satış'tan önce gelen) **Serbest Meslek & Proje**'ye geçildi (`033_freelance_proje.sql`'deki 6 tablo — migration zaten production'da mevcut). Taşınanlar: **Müşteriler** (freelance_clients CRUD), **Projeler** (durum filtreli liste + ilerleme çubuğu), **Yeni Proje** dialogu (müşteri seç/hızlı-ekle → proje adı/bütçe/saatlik ücret/teslim tarihi → ödeme planı: "Tek Seferlik" tek bir hakediş otomatik oluşturur, "Aşama Aşama" dinamik hakediş satırları + bütçeyle toplam karşılaştırma uyarısı), **Proje Detay** (`/esnaf/freelance/projeler/[id]` — durum değiştirme, Hakediş Aşamaları ödendi/ödenmedi toggle'ı `paid_amount`'ı yeniden hesaplıyor, Görevler checkbox'ı `completion_percentage`'ı yeniden hesaplıyor — mobildeki `recalcCompletion`/hakediş `markPaid` ile aynı formül —, Masraflar CRUD), **Görevler** (çapraz-proje açık görev listesi + basit başlat/durdur kronometre — mobildeki Pomodoro modu bu turda ERTELENDİ, sade bir stopwatch yeterli görüldü), **Finans & Faturalar** (Tahsil Edildi/Bekleyen özet, müşteri bazlı bekleyen bakiye, proje bazlı gelir ilerleme çubukları). **"Hızlı Fatura (PDF)" mobilde de gerçek bir özellik değil** — mobil kaynak kodunda buton sadece `"PDF Fatura özelliği yakında! (Faz 4)"` snackbar'ı gösteriyor, kopyalanacak referans yok; web'de de aynı şekilde dokunulamaz, "yakında" etiketli bir buton olarak bırakıldı (sahte bir "çalışıyor" görüntüsü verilmedi, mobildeki gerçek durumla birebir tutarlı). Yeni 6 tablo Realtime listesine + SQL migration'a eklendi.
- **19 Temmuz 2026:** Kullanıcı tekrar "devam et" dedi — Faz 3'ün son sektörü **Yüksek Hacimli Satış**'a geçildi (`028_satis_modulu.sql`'deki 4 kullanılan tablo + kullanılmayan `sale_documents` — migration zaten production'da mevcut). Taşınanlar: **Portföy** (kategori — konut/arsa/araç/kuyum/sanat/döviz/diğer, emoji'li — + durum (Satışta/Rezerve/Satıldı) CRUD, durum filtreli liste), **Müşteriler** (sale_customers CRUD), **Satış Sihirbazı** (3 adım: müşteri seç/hızlı-ekle/"Misafir Müşteri ile Devam Et" → portföyden seç veya "Manuel/Özel Ürün Gir" → toplam/kapora/ödeme türü (Tek Çekim/Taksitli) + taksit sayısı chip'leri (3/6/9/12/18/24/36) + ilk taksit tarihi + satışı yapan personel — kayıt sonrası portföy ürünü otomatik "satıldı" işaretleniyor ve **gerçek bir PDF makbuz** (yeni `jspdf` bağımlılığı, client-side, sunucu round-trip'i yok) tarayıcıdan indiriliyor), **Süreçler** (Kaporalı/Taksit/Tamamlanan 3 sekme — Taksit sekmesinde vadesi geçmiş "Gecikti" rozeti + "Ödendi" butonu, Tamamlanan sekmesi hem tek-çekim hem tüm-taksitleri-ödenmiş satışları kapsıyor). **Bilinçli olarak taşınmayan**: `sale_documents` (Evrak Arşivi) — mobil kaynakta bu ekranın evrak yükleme ikonu da sadece placeholder (`"Evrak yükleme yakında aktif olacak"` snackbar'ı), web'de Süreçler'deki ataç ikonu aynı gerçek mobil davranışı (aynı mesajla `window.alert`) koruyor, sahte bir upload akışı inşa edilmedi. Yeni 4 tablo Realtime listesine + SQL migration'a eklendi (`sale_documents` hariç, kullanılmadığı için). **Bu, Faz 3'ün 6 sektörünün de tamamlandığı anlamına geliyor** — kullanıcının "faz 3 tamamlandıysa faz 4'e geç" kuralına göre artık Faz 4 (zaten kod tarafı hazır) sırada.
- **21 Temmuz 2026:** Yatay logo için kullanıcının sağladığı gerçek PNG'ler (`fisleapp/fisle_app/fisstechlogo/*.png`, "Minimal Tech Startup Logo (Green Visor)") görüntülendi — beyaz/tek renkli, tarama-çerçevesi köşeleri + "F" harfi, transparan zemin üzerine, yani düz beyaz bir navbar'da olduğu gibi görünmez. Pixel-perfect vektör trace bir tasarım aracı gerektirdiği ve bu oturumda mevcut olmadığı için, kullanıcının KENDİ metin açıklamasıyla ("minimalist kare tarama çerçeveli 'F' simgesi") birebir eşleşen sade bir SVG (`logo-mark.tsx`) sıfırdan kuruldu — `currentColor` kullanıyor, açık/koyu temada otomatik uyum sağlıyor (orijinal PNG'nin tek-renk/tema-duyarsız olma sorunu da böylece çözülmüş oldu).
- **21 Temmuz 2026 (Faz 6 — Pazarlama Sitesi + Rebrand):** Kullanıcı fisle.co tarzı, dönüşüm odaklı bir landing page istedi; "koyu orman yeşili ve yumuşak mint tonları, mobil uygulamamızın estetiğiyle tam uyumlu" tarif etti. Araştırıldı: bu paletle BİREBİR eşleşen gerçek bir mobil tema var — `app_colors.dart`'taki "Yeşil" tema (primary `#1E4D2B`, accent `#D6EAD9`) — ama web'in TÜM geri kalanı 18 Temmuz'dan beri bilinçli olarak mobildeki "Gül" temasını (`#B23A65`) kullanıyordu. Kullanıcıya `AskUserQuestion` ile net soruldu: sadece landing page mi, yoksa site geneli mi? **Kullanıcı: "TÜM web sitesini... Yeşil temaya geçir, pazarlama sayfası ve ürün arayüzü arasında marka ve renk bütünlüğü istiyorum."** Bu, 18 Temmuz'daki "Gül" kararını GERİ ALAN, kapsamı landing page'den tüm ürüne genişleten bir karar.
- **21 Temmuz 2026:** Rebrand SADECE `accent`/`accent-hover`/`on-accent` + yeni `accent-soft` (mint, sadece dekoratif zemin/rozet — üzerine metin konulmaz, kontrast yetersiz) token'larını değiştirdi; `bg`/`surface`/`text-*`/`border`/`success`/`danger`/`warning` bilinçli olarak DOKUNULMADI (kapsamlı palet yeniden yazımı değil, hedefli marka-rengi değişimi). Yeni hex'ler WCAG AA hesaplanarak seçildi: açık mod accent `#1E4D2B` beyaz üzerinde 9.76:1, bg üzerinde metin/link olarak 8.98:1; koyu mod accent mobilin "Karanlık" temasındaki Material yeşili `#4CAF50` — koyu bg üzerinde 6.52:1, Gül'de olduğu gibi on-accent koyu tutuldu (beyaz değil, çünkü accent zaten parlak). Token'lar tek noktadan değiştiği için mevcut TÜM sayfalar (dashboard, 20+ modül) hiçbir component düzenlenmeden otomatik yeşile döndü — token-driven mimarinin tam olarak bunun için kurulduğu doğrulandı. **İstisna:** recharts SVG grafiklerinde `fill`/`stroke` literal hex string gerektirdiği için 5 dosyada (`net-balance-trend-chart.tsx`, `balance-trend-chart.tsx`, `trend-line-chart.tsx`, `store-breakdown-chart.tsx`, `kafe-menu-analytics.tsx`) tekil-seri "bu marka rengini temsil ediyor" kullanımları `"var(--color-accent)"` string'ine çevrildi (bu dosyalarda zaten `stroke="var(--color-border)"` gibi aynı desen kullanılıyordu). **Bilinçli olarak DOKUNULMAYAN:** `analytics.ts`/`store-breakdown-chart.tsx`'teki çok-renkli kategorik `CHART_COLORS` paletleri — ilk rengi yeşile çevirmek, paletteki `success` (`#2E7D32`) ile yan yana iki benzer yeşil oluşturup grafik okunabilirliğini bozardı; bu paletler "N kategoriyi ayırt etme" amaçlı, "marka rengini yansıtma" amaçlı değil.
- **21 Temmuz 2026:** `/` artık herkese açık — `src/lib/supabase/middleware.ts`'e `PUBLIC_ROUTES = ["/"]` eklendi (AUTH_ROUTES'tan farklı: oturumlu ziyaretçi buradan ATILMIYOR, sayfa kendi CTA'sını uyarlıyor — "Uygulamaya Git" hero butonunun anlamlı olması için gerekliydi). `src/app/page.tsx` artık `redirect("/dashboard")` yerine gerçek bir Server Component, `supabase.auth.getUser()` ile `isAuthenticated` durumunu Navbar/Hero'ya prop olarak geçiriyor.
- **21 Temmuz 2026:** Ücretsiz Fiş Tarama demosu kullanıcı kararıyla SİMÜLE edildi (gerçek Gemini OCR çağrısı yok, anonim Supabase auth yok) — araştırmada `scan-receipt`'in kredi sistemi (`consumeScanCredit`) gerçek bir `userId`'ye bağlı olduğu, anonim bir yol olmadığı doğrulandı; gerçek OCR isteseydik anonim-auth + IP bazlı ek limitleme gerekirdi. Bunun yerine 4 önceden yazılmış gerçekçi örnek sonuç (`src/lib/landing/demo-receipts.ts`) sırayla gösteriliyor, `localStorage`'da 5 hak sayacı tutuluyor.
- **21 Temmuz 2026:** AI Destek Widget için `fisle-ai-chat` (mevcut, JWT zorunlu, kullanıcının gerçek harcama verisine bağlam olarak erişen) edge function'ı YENİDEN KULLANILMADI — anonim bir pazarlama ziyaretçisi için hem yanlış yetki modeli hem de o edge function'ın kaynağı bu repoda bile yok (deploy edilmiş, ayrı bir yerde). Bunun yerine yeni, public bir Next.js Route Handler yazıldı: `src/app/api/landing-chat/route.ts` — sabit "sadece Fişştech ürünü hakkında, kullanıcı verisine erişimi yok" sistem promptuyla Gemini'yi doğrudan (yeni SDK bağımlılığı eklemeden, düz `fetch`) çağırıyor, `GEMINI_API_KEY` sadece sunucu tarafında (AGENTS.md kural 7). Basit IP bazlı in-memory rate limit (10 istek/10dk) eklendi — sunucusuz ortamda instance'lar arası paylaşılmaz ama tek-instance/düşük-trafik için anlamlı bir sınır. **`GEMINI_API_KEY` henüz sağlanmadı** — widget şu an "yapılandırılmamış" mesajı gösteriyor, site bu yüzden kırılmıyor ama widget gerçek çalışmıyor, bkz. Açık Sorular.
- **21 Temmuz 2026:** Fiyatlandırma bölümü mobil kaynaktaki (`paywall_screen.dart`) GERÇEK plan adları/fiyatları kullandı: Premium ₺49,99/ay, Esnaf Modu ₺199,99/ay (+ 10/20/40 personel katmanı eklentileri), Aile Planı ₺119,99/ay, Yedekleme eklentisi ₺29,99/ay (sadece aylık). Kullanıcının bahsettiği "Standart" adında bir plan mobil kaynakta YOK (muhtemelen Premium kastedildi, kullanıcıya soruldu ve onaylandı). **Not:** bu fiyatlar mobil Dart kodunda elle girilmiş display string'leri — gerçek App Store/Play Store fiyatıyla birebir aynı olduğu garanti değil, yayına almadan önce spot-check gerekir (bkz. Açık Sorular). Web'de gerçek ödeme altyapısı olmadığı için (RevenueCat sadece mobil IAP) kartlardaki CTA'lar `/register`'a gidiyor, bilgilendirme amaçlı.
- **21 Temmuz 2026:** SSS accordion için projede önceden bir primitive yoktu — `src/components/modules/landing/faq-section.tsx`'te sıfırdan, erişilebilir (buton + `aria-expanded`/`aria-controls`, tek seferde bir soru açık) ve JS yükseklik ölçümü gerektirmeyen CSS `grid-template-rows: 0fr→1fr` tekniğiyle inşa edildi.
- **21 Temmuz 2026:** Scroll-reveal/hover mikro-etkileşimler için `framer-motion` gibi yeni bir animasyon kütüphanesi EKLENMEDİ — projenin bu oturum boyunca kurduğu "gereksiz bağımlılık ekleme" disiplinine (xlsx/jspdf lazy-load, performans denetimi notları) uyularak `IntersectionObserver` + CSS `transition` tabanlı, sıfır-bağımlılık bir `Reveal` sarmalayıcı yazıldı (`prefers-reduced-motion`'a saygılı). Hover lift/shadow efektleri düz Tailwind `hover:` class'larıyla yapıldı, ek kod gerekmedi.
- **21 Temmuz 2026:** İki `set-state-in-effect` lint hatası (yeni `Reveal` ve `ScanDemo` bileşenlerinde, `matchMedia`/`localStorage` gibi SSR'da erişilemeyen tarayıcı state'ini mount sonrası okuma girişiminden) projenin zaten kurduğu `useSyncExternalStore` tabanlı `useMounted()` deseniyle (`theme-toggle.tsx`/`appearance-card.tsx`'te önceden var) çözüldü — effect içinde senkron `setState` yerine, `mounted` bayrağına göre RENDER SIRASINDA doğrudan okuma yapıldı (hydration mismatch yaratmadan, çünkü ilk sunucu+istemci pass'i ikisi de `mounted=false` ile aynı çıktıyı üretiyor).
- **21 Temmuz 2026:** Kullanıcının iki `npx claude-code-templates@latest --skill ...` komutuyla istediği `creative-design/frontend-design` ve `development/senior-frontend` skill şablonları `.claude/skills/`'e kuruldu, kodlamaya başlamadan önce okundu — `frontend-design`'daki "jenerik AI estetiğinden kaçın, bold bir yöne commit ol, arkaplanlarda atmosfer/derinlik kur" ilkeleri hero'daki scan-frame motifli mockup, özellik kartlarındaki hover-lift, fiyat kartlarındaki gradient banner gibi kararlara yansıtıldı.
- **21 Temmuz 2026 (Faz 6 devamı — landing page'in geri kalanı da "premium SaaS" kalitesine çekildi):** Kullanıcı fiyatlandırma kartlarının kalitesini be­ğenince aynı özeni sayfanın geri kalanına da uygulamamı istedi (`AskUserQuestion` ile netleştirildi). Section bazında: **Hero** — arka plana çok silik bir nokta-ızgara dokusu (`radial-gradient` + `maskImage` ile kenarlara doğru soluyor) eklendi, mockup kartının altına dışa taşan/üst üste binen küçük bir "2.1 saniyede okundu" rozet-kartı kondu (derinlik hissi için — frontend-design skill'in "overlap" ilkesi), CTA'lara `hover:scale`, alta gerçek olmayan sayısal istatistik UYDURULMADAN (ör. "10.000+ kullanıcı" gibi doğrulanamayan bir rakam eklenmedi) iki nitel güven rozeti ("Saniyeler içinde tarama", "RLS ile uçtan uca güvenli") eklendi. **Özellikler** — tekdüze 4'lü ızgara yerine "bento" düzenine geçirildi: Akıllı Fiş Taraması artık tam genişlikte, gradyanlı, öne-çıkan bir kart; diğer 3 özellik altında küçük kart olarak duruyor. **Nasıl Çalışır** — her adım artık gerçek bir kart (border+hover), arkasında büyük soluk "01/02/03" hayalet rakamlar var (yaygın premium SaaS motifi). **SSS** — ChevronDown yerine tıklanınca 45° dönüp X'e dönüşen dairesel "+" rozeti, altına "Hâlâ sorunuz mu var?" destek çağrısı kartı eklendi (AI widget'a link YOK — ayrı bileşenler arası state paylaşımı gerektirirdi, sadece görsel yönlendirme). **Navbar** — scroll'da beliren ince gölge (`useEffect` + `scroll` event listener, `set-state-in-effect` kuralına takılmadı çünkü setState callback İÇİNDE, effect gövdesinde değil), nav linklerinde CSS-only animasyonlu alt çizgi (`after:` pseudo-element). Tüm yeni gölge/glow efektleri `color-mix(in srgb, var(--color-accent) ...)` ile token'a bağlı kaldı, hiçbir yerde hardcoded hex kullanılmadı. Değişiklikler sonrası tüm etkileşim testi (SSS aç, fiyat toggle, demo 5 hakkın tamamı, AI widget) sıfır konsol hatasıyla yeniden çalıştırıldı, 3 breakpoint + açık/koyu modda ekran görüntüsüyle doğrulandı.
- **21 Temmuz 2026 (Faz 6 devamı — Fiyatlandırma kartları "Premium SaaS" tipografisine geçirildi):** Kullanıcı kartların yapısını fisle.co tarzı bir premium SaaS standardına çekmemi istedi: "En Popüler" rozeti artık kartın border-top'una ortalanmış, dışa taşan (`absolute -top-4 left-1/2 -translate-x-1/2`) bir pil; fiyat tipografisi devasa/kalın rakam (`text-6xl font-bold`) + küçük/silik para birimi ve `/ay` (`text-text-secondary` — kullanıcı örnekte `text-gray-400` önermişti ama proje kuralı gereği token'a bağlı kalındı, hardcoded Tailwind gri kullanılmadı) + rakamın altına silik microcopy ("Aylık faturalandırılır · vergiler dahildir" / yıllıkta mevcut tasarruf metni); "Hemen Başla" butonu artık fiyatın hemen altına, tam genişlikte taşındı (Premium = dolu yeşil, diğer ikisi = şeffaf zemin + ince çerçeve + silik metin, `buttonVariants`'a özel className ile — paylaşılan `button.tsx`'e yeni bir varyant eklenmedi, sadece bu ekranda gerekli olduğu için); özellik listesi satır aralığı `space-y-2.5`→`space-y-4` açıldı; Premium kartına markanın accent rengiyle `color-mix(in srgb, var(--color-accent) 55%, transparent)` kullanan CSS `boxShadow` glow efekti eklendi (yine token'a bağlı, hardcoded hex yok). Fiyat verisi (`pricing-data.ts`) artık para birimini AYRI tutuyor (`monthly: "49,99"`, ₺ component'te render ediliyor) — bu da önceki turdaki "/ay/ay" çift-etiket hatasının kök nedenine benzer bir string-birleştirme hatasına düşmeden büyük/küçük tipografiyi ayrı span'lara bölebilmek için gerekliydi. Alttaki "Bulut Yedekleme" eklenti kartı ve emoji ikonlar kullanıcı isteğiyle AYNEN korundu, dokunulmadı.
- **21 Temmuz 2026 (Faz 6 devamı — gerçek logo + etkileşimli doğrulama):** Kullanıcı elle çizilmiş SVG logoyu (F harfi + tarama çerçevesi path'leri) reddetti, kendi sağladığı gerçek PNG'leri (`Fisstech_web/public/fişştech logo web/*.png`, "Minimal Tech Startup Logo (Green Visor)") kullanmamı istedi. İki sorun bulundu ve çözüldü: (1) kaynak PNG'ler 2500×2500 kanvasta bol şeffaf boşluklu (F ikonu gerçek içerik alanı 1415×1646, kelime markası 1205×326) — sabit next/image boyutlarıyla kullanılsaydı kelime markası orantısız küçük görünürdü; hiçbir image-processing kütüphanesi (ImageMagick/PIL/sharp) kurulu olmadığı için zaten mevcut olan Playwright bir canvas'a çizip alpha bounding box'a göre otomatik kırpmak için kullanıldı (yeni bağımlılık eklenmeden) — kırpılmış çıktılar `public/logo-f-icon.png`/`public/logo-wordmark.png`'a yazıldı. (2) İkisi de düz BEYAZ sanat eseri, şeffaf zemin üstüne — açık modda (beyaz navbar) görünmez olurdu; `invert dark:invert-0` Tailwind class'ıyla açık modda koyulaştırılıp koyu modda olduğu gibi bırakıldı, şeffaflık korunarak. Kaynak PNG'lerin bulunduğu `public/fişştech logo web/` klasörü de taşındı: `public/` Next.js tarafından doğrudan web'den servis edildiği için, üzerinde stok şablon paketinin orijinal dosya adları ("Minimal Tech Startup Logo") yazan ham kaynak dosyaları herkese açık kalmasın diye repo köküne (`design-assets/`, servis edilmeyen) taşındı.
- **21 Temmuz 2026:** Kullanıcı "4 ve 5'i de tamamla" dedi (daha önce kendi önerdiğim ama henüz yapmadığım iki doğrulama adımı: gerçek tıklama etkileşimi + tablet kırılımı). Playwright ile gerçek bir etkileşim testi yazıldı (SSS'de bir soruyu tıklayıp açma, fiyatlandırmada yıllık switch'e tıklama, tarama demosuna sahte bir dosya yükleyip "taranıyor" animasyonunun ve sonucun gerçekten göründüğünü doğrulama, aynı akışı 5 kez tekrarlayıp tam kilitlenme mesajının çıktığını doğrulama, AI widget'ı açma) + 768px tam sayfa ekran görüntüsü. Bu, iki gerçek hata buldu (yazı/görsel doğrulamayla değil, gerçek etkileşimle ortaya çıkan türden): (1) 768px'te (`md:` eşiği tam burada devreye giriyordu) masaüstü navbar'ı logoya çok yakın sıkışıp "FişştechÖzellikler" gibi bitişik görünüyordu — eşik `md:` (768px) yerine `lg:` (1024px) yapıldı, 768 artık kompakt/hamburger görünümde kalıyor. (2) Fiyatlandırma'da yıllık görünümde "₺33,33/ay/ay" gibi çift birim etiketi çıkıyordu — `pricing-data.ts`'teki `yearlyEffective` alanları zaten "/ay" içeriyordu ama component ayrıca kendi "/ay" span'ını da ekliyordu; veri alanlarından fazladan "/ay" kaldırıldı. Düzeltmeler sonrası testler tekrar çalıştırıldı, sıfır konsol hatasıyla ve doğru görünümle geçti.
- **19 Temmuz 2026:** Kullanıcı `047_web_realtime.sql`'i Supabase SQL Editor'da çalıştırdı, ilk satırda (`expenses`) `ERROR: 42710: relation "expenses" is already member of publication "supabase_realtime"` hatası aldı — `expenses` tablosu daha önce (muhtemelen elle, Dashboard üzerinden) publication'a zaten eklenmiş, script bunu bilmediği için tek düz `ALTER PUBLICATION ADD TABLE` listesiyle ilk hatada tamamen duruyordu, geri kalan ~40 tablo hiç eklenmedi. Script, mobilin kendi 022/023 migration'larında zaten kullandığı `pg_publication_tables` kontrol deseni genelleştirilerek **tam idempotent** bir `DO $$ ... FOREACH ... END $$` bloğuna çevrildi — her tablo eklenmeden önce zaten publication'da olup olmadığı kontrol ediliyor, varsa sessizce atlanıyor. Artık script'in tamamı tek seferde, hatasız ve defalarca güvenle çalıştırılabilir. Kullanıcı ilk denemede yine eski (idempotent olmayan) script içeriğini SQL Editor sekmesinde çalıştırıp aynı hatayı tekrar aldı — sorun dosyanın değil, editördeki eski sekme içeriğinin güncellenmemiş olmasıydı; güncel dosya içeriği doğrudan sohbette verilip editöre yeniden yapıştırılması istendi. **Kullanıcı script'i başarıyla çalıştırdığını doğruladı — Faz 4 artık gerçekten AKTİF**, sadece kod tarafı değil, Supabase tarafındaki `supabase_realtime` publication'ı da tüm ~40 tabloyu kapsıyor.

---

## Açık Sorular / Bekleyen Kararlar

- **Raporlar sayfası (yeni Market Dağılımı grafiği, Hedefler ilerleme çubuğu, print/PDF görünümü) ve Sidebar/Topbar'daki yeni navigasyon öğeleri gerçek bir kullanıcı oturumuyla henüz tarayıcıda görsel doğrulanmadı** — bu ortamda test hesabı/oturum yok. Kod tarafı tsc/lint/build temiz; print CSS mekanizması dolaylı olarak `/login`'de (aynı token sistemi, koyu modda print emülasyonu) doğrulandı. Kullanıcının gerçek verisiyle bir "Yazdır/PDF Kaydet" denemesi ve Sidebar/Topbar'daki yeni öğelerin gerçek hesapta görünüp doğru çalıştığının teyidi bekleniyor.
- ~~Esnaf Modu — 6 sektörel modülün kendine özgü ekranları yok.~~ **Çözüldü (19 Temmuz 2026) — 6 sektörün 6'sı da tamamlandı**, bkz. Kararlar Günlüğü.
- **Esnaf Modu — premium gating yok.** Herhangi bir giriş yapmış kullanıcı erişebiliyor; mobildeki `esnaf_plan === 'esnaf_premium'` kısıtı web'de henüz uygulanmadı (ödeme akışı olmadığı için kasıtlı).
- ~~Esnaf Modu — işletme logosu/adres alanları formda yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- **Esnaf Modu — "Mağaza Verilerini Sıfırla" / "Mağazayı Tamamen Sil" (business_settings_screen.dart) web'e taşınmadı.** Bilinçli olarak ERTELENDİ: mobil kaynaktaki `resetBusinessData()` kendi içinde tutarsız/eksik (`business_invoices`, `stocks`, `sale_processes` gibi artık gerçek şemada karşılığı olmayan tablo adları kullanıyor, perakende/hizmet/toptan/freelance tablolarının hiçbirini temizlemiyor) — bu, güvenilir bir referans olmadan kopyalanamayacak, YIKICI (geri alınamaz veri silme) bir özellik. Doğru/eksiksiz bir "tüm sektör tablolarını temizle" implementasyonu ayrı, dikkatli bir incelemeyle ele alınmalı.
- **Evrak Arşivi'nde OCR doğruluğu sınırlı** — `scan-receipt` fiş için optimize; genel evrak/fatura için özel bir edge function (ayrı prompt) daha doğru sonuç verirdi. Şimdilik kullanıcı düzenleme adımıyla telafi ediliyor.
- ~~Menü Yönetimi'nin geri kalanı (masa planı, adisyon/sipariş, paket servis, ödeme alma) yok.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Bu, Esnaf Modu'nun planlanan tüm büyük alt sistemlerinin tamamlandığı anlamına geliyor.**
- **Menü ekstraları (`menu_item_extras`) için yönetim UI'ı yok.** Şema hazır (GRANT+RLS zaten production'da) ama ürün ekleme formunda ekstra tanımlama alanı yok, bu yüzden sipariş alma akışında da ekstra seçimi bilinçli olarak dahil edilmedi (mobilde var olan bir özellik, kapsam dışı bırakıldı — bkz. Kararlar Günlüğü).
- **Restoran ödeme akışında item-bazlı kısmi ödeme ("parçalı" mod) yok.** Mobil kaynakta bu özellik TAM olarak kodlanmış ama hiçbir UI'dan seçilemiyor (ölü/ulaşılamayan kod) — web de aynı şekilde bu modu sunmuyor, bilinçli bir karar (bkz. Kararlar Günlüğü).
- Fiş yükleme alanı için özel illüstrasyon/mockup (blueprint §5.4 "imza öğesi") henüz yok — şu an ikon kompozisyonu placeholder olarak kullanılıyor, kod içinde TODO olarak işaretli.
- GitHub remote bağlantısı henüz kurulmadı — sadece local git repo (kullanıcı isteğiyle, ileride ele alınacak).
- Deployment hedefi (Vercel) henüz kesinleşmedi/kurulmadı.
- Fiş Tara akışının gerçek bir fiş fotoğrafıyla uçtan uca (yükle → tara → onayla → kaydet) testi kullanıcı tarafından henüz teyit edilmedi — Dashboard doğrulandı ama bu adım ayrıca kontrol edilmeli.
- **AI Sohbet — Gemini API 429 (kota) hatası.** Kod tarafı düzeldi (model adı güncellendi), ama Google AI Studio/Cloud tarafındaki `GEMINI_API_KEY`'in kota/faturalandırma durumu kontrol edilmeli. Kullanıcı kararıyla ertelendi.
- ~~Gruplarım — davet/katılma akışı yok.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- ~~Gruplarım — üye listesi, sohbet yok.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- **AI Destek Widget (landing page) — `GEMINI_API_KEY` env değişkeni sağlanmadı.** Kod tarafı tamamen hazır (`src/app/api/landing-chat`) ve key yokken zarifçe "yapılandırılmamış" mesajı gösteriyor (site kırılmıyor) — ama widget gerçekten çalışmıyor. Kullanıcının bir Gemini API key sağlaması (veya mevcut `fisle-ai-chat`'in kullandığı key'in — farklı bir Google Cloud projesi/kota altında olması önerilir, aynı kotayı paylaşıp AI Sohbet'i etkilememesi için — paylaşılması) bekleniyor.
- **Fiyatlandırma bölümündeki TL fiyatları mobil Dart kodundan alındı, App Store/Play Store'daki GÜNCEL fiyatla spot-check edilmedi.** Yayına almadan önce kontrol edilmeli (bkz. Kararlar Günlüğü).
- **Mobil (Flutter) tarafı bu yeni yeşil/mint marka kararına göre henüz güncellenmedi.** Kullanıcı "şimdilik sadece web" dedi — mobilde zaten bir "Yeşil" tema seçeneği var (varsayılan tema da o), ileride web ile tam eşleşmesi/tek tema yapılması ayrı bir karar olarak bekliyor.
- ~~Navbar logosu elle çizilmiş SVG'ydi, gerçek logo dosyaları kullanılmıyordu.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. Landing page'deki hero mockup'ı ("Migros Kampüs" fiş önizlemesi) hâlâ kod-tabanlı (gerçek bir fiş görseli değil, bilinçli bir tasarım kararı — bkz. judgment call D).
- ~~Gruplarım — harcama paylaştırma (split) ve yorum/reaksiyon.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Bu, Gruplarım'ın (mobildeki en büyük tek özellik) web'e tamamen taşındığı anlamına geliyor** — settlement/"kim kime borçlu" hesaplaması hariç (mobilde gerçekten bozuk/hiç ulaşılamayan kod yolu, bilinçli olarak kopyalanmadı, bkz. Kararlar Günlüğü).
- ~~Hedeflerim — `savings_pool` (genel birikim havuzu) akışı yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Kullanıcı eylemi gerekiyor:** `docs/sql/047_web_realtime.sql`'e `savings_pool` eklendi, script daha önce çalıştırılmıştı — Realtime'ın bu tabloyu da kapsaması için Supabase SQL Editor'da yeniden çalıştırılmalı (idempotent, sadece yeni tabloyu ekler, hata vermez).
- ~~Yatırımlarım — canlı piyasa fiyatı yok.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Kalan:** `price_alerts` (fiyat alarmı) UI'ı hâlâ web'e bağlanmadı.
- ~~Gruplarım — gerçek hesapta üyesi olunan bir grup web'de "üye değilim" gösteriyor.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. Kök neden RLS değil, Dashboard'ın `family_memberships` (var olmayan/terk edilmiş bir Faz-1 placeholder tablosu) okuyan `getGroupMembershipCount()`'uydu.
- **Ayarlar — Premium'a Geç / Esnaf Aboneliğine Geç yok.** Mobil RevenueCat (native in-app-purchase) kullanıyor, web'in hiçbir ödeme işlemcisi (Stripe vb.) yok — bu kod yazarak çözülemez, kullanıcının bir ödeme altyapısı kararı vermesi gerekiyor.
- **Ayarlar — "Bir İşyerine Katıl" yok.** Bu, daha önce Hizmet & Bakım turunda da gündeme gelen ayrı ve büyük bir "workspace"/çoklu-personel alt sistemi (bir çalışanın kendi hesabıyla giriş yapıp bir işletmeye bağlanması) — mevcut `employees` tablosu sadece işveren tarafından isim/maaş takibi için, gerçek bir çoklu-kullanıcı yetkilendirme modeli değil. Kapsam dışı bırakıldı, ayrı bir oturumda ele alınmalı.
- ~~Özet Oluştur — Excel export yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- AI Sohbet hatasının kesin sebebi henüz doğrulanmadı — düzeltilen hata-mesajı gösterimiyle bir sonraki canlı testte netleşecek (muhtemel adaylar: `fisle-ai-chat` fonksiyonunda `GEMINI_CHAT_API_KEY`/`GEMINI_API_KEY` secret'ının tanımlı olmaması ya da Gemini model/kota hatası).
- ~~Abonelikler Dashboard kutusu hâlâ gerçek veriye bağlanmadı.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- Yeni eklenen tüm modüllerin (Harcamalarım, Gelirlerim, Borçlarım, Hedeflerim, Yatırımlarım, Abonelikler, Gruplarım, Analizler, AI Sohbet, Notlar, Ayarlar) gerçek kullanıcı hesabıyla tarayıcıda uçtan uca teyidi henüz yapılmadı — build/lint/type-check temiz ama görsel/fonksiyonel doğrulama bekliyor.
- **Borçlarım — Hatırlatıcı tarayıcı bildirimi göndermiyor.** Toggle + vade tarihi kaydediliyor (panoda "Bugün/Yarın/Dün" olarak vurgulanıyor) ama gerçek bir push bildirimi tetiklemiyor — mobildeki yerel (`flutter_local_notifications`) bildirim mimarisi cihaz-yerel, web karşılığı Service Worker + Web Push API + bir zamanlanmış gönderim mekanizması (örn. Supabase Edge Function + `pg_cron`) gerektirir; bu ayrı bir altyapı işi olarak ertelendi.
- ~~Harcama formunda özel kategori ekleme yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- Harcamalarım/Gelir-Gider/Borçlarım'daki yeni Dönem Seçin filtresi, taksit akışı, Gruba Ekle ve Excel export'un gerçek kullanıcı hesabıyla tarayıcıda uçtan uca teyidi henüz yapılmadı — build/lint/type-check temiz.
- **Abonelikler — Hatırlatmaları Aç toggle'ı da gerçek bildirim göndermiyor** (Borçlarım'daki Hatırlatıcı ile aynı altyapı eksikliği — bkz. yukarıdaki madde).
- ~~Analiz — Genel Kategoriler sekmesinde taksitli harcamaların parent_group ataması doğrudan doğrulanmadı.~~ **Doğrulandı (20 Temmuz 2026)** — bkz. Kararlar Günlüğü. Sonuç: web'in mevcut davranışı DEĞİŞTİRİLMEDİ, mobildeki tutarsızlık kasıtlı olarak kopyalanmadı.
- ~~Analiz — Bütçe/Limit kartı yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- Taksit ödeme takvimi (`/expenses/[id]`), 4 sekmeli Analiz sayfası ve genişletilmiş Abonelik formunun gerçek kullanıcı hesabıyla tarayıcıda uçtan uca teyidi henüz yapılmadı — build/lint/type-check temiz.
- ~~Hızlı Perakende — Satış Geçmişi yok.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- **Hızlı Perakende — Perakende Giderleri, Ayarlar ekranları bilinçli olarak taşınmadı.** Mobil kaynak incelendiğinde ikisinin de web'de zaten var olan işlevlerin tekrarı olduğu görüldü: "Giderler" ekranı sadece `business_expenses`'ın (Kasa Defteri'nde zaten tam CRUD'u olan) ay bazlı salt-okunur bir görünümü; "Ayarlar" ekranı da sadece diğer ekranlara (Müşteriler/Personel/Stok/Evrak/Raporlar/İşletme Ayarları) kısayollardan oluşan bir menü — web'in kalıcı sekme çubuğu bunların hepsini zaten doğrudan sunuyor, ayrı bir menü sayfasına gerek yok.
- ~~Hızlı Perakende — PLU kod arama, ürün varyasyonları.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- ~~Hızlı Perakende — gün sonu toplu fiş tarama.~~ **Çözüldü (21 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Bu, Hızlı Perakende'nin altı eksik alt-özelliğinin hepsinin tamamlandığı anlamına geliyor** (Ayarlar ve Giderler bilinçli olarak atlandı, gerekçesi yukarıda).
- **Hızlı Perakende'nin gerçek kullanıcı hesabıyla (gerçek işletme + ürün + satış) tarayıcıda uçtan uca teyidi henüz yapılmadı** — build/lint/type-check temiz.
- ~~Hizmet & Bakım — sektöre özel Pano yok (Akıllı Bildirimler) + usta primleri.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü. **Düzeltme:** bir önceki oturumda "usta primleri `workspace`/çoklu-personel özelliğinin bir parçası, Hizmet'e özgü değil" denmişti — bu YANLIŞTI. Daha derin inceleme, primin gerçekte Atölye'nin "İşi Tamamla" akışına bağlı olduğunu gösterdi (`atolye_screen.dart`), gerçekten Hizmet & Bakım'a özgü ve artık portlandı.
- **`staff_commissions` tablosu için migration mobil kaynakta hiç yok** — `docs/sql/048_staff_commissions.sql` web tarafından YENİ yazıldı (tablo muhtemelen Supabase Dashboard'dan elle oluşturulmuştu, migration dosyası hiç izlenmemiş). GRANT+RLS zorunluluğu kuralına uyularak `CREATE TABLE IF NOT EXISTS` + GRANT + 4 RLS policy + Realtime publication kaydı içeriyor — zaten varsa hiçbir şeyi bozmaz. **Kullanıcı eylemi gerekiyor:** bu dosyanın Supabase SQL Editor'da çalıştırılması gerekiyor, yoksa prim kaydetme "relation does not exist" hatası verir.
- ~~Hizmet & Bakım — randevu iptal/yeniden zamanlama.~~ **Doğrulandı (20 Temmuz 2026), kod değişikliği gerekmedi:** `ajanda_screen.dart` incelendi — mobilin kendisi de dolu bir slota tıklamayı `onTap: hasAppt ? null : onTap` ile devre dışı bırakıyor, randevu iptal/yeniden zamanlama mobilde de YOK. Web zaten mobille birebir aynı (salt okunur) davranıyor.
- **Hizmet & Bakım — iş emri detay/düzenleme ekranı yok.** Atölye'de kartlar sadece ileri yönde durum değiştirebiliyor, geri alma veya detay/düzenleme yok. (Not: "Tamamla" adımı artık zengin bir tamamlama dialogu açıyor — bkz. usta primleri maddesi — ama genel bir "işi aç, her alanı düzenle" ekranı hâlâ yok, mobil kaynakta da böyle bir ekran bulunamadı.)
- **Hizmet & Bakım'ın gerçek kullanıcı hesabıyla (gerçek işletme + müşteri + randevu + iş emri) tarayıcıda uçtan uca teyidi henüz yapılmadı** — build/lint/type-check temiz.
- ~~Faz 4 — `docs/sql/047_web_realtime.sql` kullanıcı tarafından Supabase Dashboard'da henüz ÇALIŞTIRILMADI.~~ **Çözüldü (19 Temmuz 2026) — kullanıcı script'i (idempotent versiyonla) başarıyla çalıştırdı, Realtime artık gerçekten aktif.**
- **Faz 4 — gerçek zamanlı senkronizasyonun gerçek kullanıcı hesabıyla (iki sekme/cihaz açıp bir yerde değişiklik yapıp diğerinde otomatik yenilendiğini görme) uçtan uca teyidi henüz yapılmadı.**
- **Toptancı & İmalatçı — imalat/üretim takibi yok.** Mobil isminde "İmalatçı" geçse de mobil kaynakta ayrı bir üretim/BOM (bill of materials) akışı bulunamadı; bu modül aslında toptan satış + depo + cari hesap odaklı, web tarafı da bununla tutarlı.
- **Toptancı & İmalatçı — sipariş onaylanınca stok otomatik düşmüyor.** Mobil kaynakta bu davranışın ne zaman/nasıl tetiklendiği net değildi (rapor bunu doğrulayamadı), bu yüzden icat edilmedi — Depo stoğu şimdilik sadece elle +/- düzeltmeyle değişiyor. Gerekirse mobil ekip netleştirdikten sonra eklenebilir.
- **Toptancı & İmalatçı'nın gerçek kullanıcı hesabıyla (gerçek işletme + bayi + sipariş + sevkiyat) tarayıcıda uçtan uca teyidi henüz yapılmadı** — build/lint/type-check temiz.
- ~~Serbest Meslek & Proje — Pomodoro modu yok.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- **Serbest Meslek & Proje — "Hızlı Fatura (PDF)" hiç çalışmıyor (kasıtlı — mobilde de çalışmıyor).** Gerçek bir PDF fatura üretimi hem mobilde hem web'de yok; buton "yakında" etiketiyle devre dışı bırakıldı.
- ~~Serbest Meslek & Proje — AI Masraf Tarayıcı taşınmadı.~~ **Çözüldü (20 Temmuz 2026)** — bkz. Kararlar Günlüğü.
- **Serbest Meslek & Proje — sektöre özel Pano yok** (Hizmet & Bakım'daki aynı kapsam kararıyla tutarlı) — genel `/esnaf/pano` kullanılıyor, "Bu Ay Kesilecek Faturalar" hero kartı ve vade-yaklaştı uyarı rozeti taşınmadı.
- **Serbest Meslek & Proje'nin gerçek kullanıcı hesabıyla (gerçek işletme + müşteri + proje + görev + zaman kaydı) tarayıcıda uçtan uca teyidi henüz yapılmadı** — build/lint/type-check temiz.
- **Yüksek Hacimli Satış — Evrak Arşivi (`sale_documents`) taşınmadı.** Mobil kaynakta bu ekranın evrak yükleme ikonu da sadece `"Evrak yükleme yakında aktif olacak"` snackbar'ı gösteriyor (kopyalanacak gerçek implementasyon yok) — web'de Süreçler/Taksit satırlarındaki ataç ikonu aynı gerçek mobil davranışla `window.alert` ile aynı mesajı gösteriyor, sahte bir upload akışı kurulmadı.
- **Yüksek Hacimli Satış — PDF makbuz web'e özgü bir çözümle (jsPDF, client-side) yeniden inşa edildi**, mobildeki native PDF kütüphanesi + paylaşım sheet'i yerine — makbuz tarayıcıdan doğrudan indiriliyor. İçerik/alanlar mobildekiyle aynı (tarih/müşteri/ürün/tutar/kapora/ödeme türü/taksit planı) ama görsel tasarım/marka şablonu birebir kopyalanmadı (sade, metin ağırlıklı bir düzen).
- **Yüksek Hacimli Satış — sipariş/taksit personel prim hesabı yok**, sadece "Satışı Yapan" alanı kaydediliyor, komisyon/prim mantığı web'e taşınmadı (mobil kaynakta da bu sektöre özgü net bir prim tablosu tespit edilmedi).
- **Yüksek Hacimli Satış'ın gerçek kullanıcı hesabıyla (gerçek işletme + portföy + müşteri + satış + taksit + PDF indirme) tarayıcıda uçtan uca teyidi henüz yapılmadı** — build/lint/type-check temiz.
- **Faz 3 TAMAMLANDI — 6 sektörün 6'sı da web'de.** Kullanıcının kendi kuralına göre ("faz 3 tamamlandıysa faz 4'e geç") artık Faz 4'e geçilebilir — kod tarafı zaten hazır (bkz. üstteki Genel Durum), tek kalan iş `docs/sql/047_web_realtime.sql`'in Supabase Dashboard'da çalıştırılması ve gerçek kullanıcı hesabıyla uçtan uca teyit.

- **20 Temmuz 2026:** Kullanıcı "hepsini sırayla yap" dedi (Faz 5 → gerçek hesapla tarayıcı doğrulaması → Açık Sorular listesi, sırayla) — Faz 5'in erişilebilirlik denetimiyle başlandı. Icon-only butonlarda eksik `aria-label` taraması yapıldı, 4 esnaf dosyasında (`freelance-project-detail.tsx`, `freelance-project-dialog.tsx`, `hizmet-sihirbaz-dialog.tsx`, `toplu-siparis.tsx`) 6 eksik etiket eklendi. Paylaşılan `src/components/ui/dialog.tsx` (uygulamadaki onlarca dialog'un ortak bileşeni) klavye erişilebilirliği için yeniden yazıldı: Tab/Shift+Tab focus trap, açılışta ilk odaklanabilir öğeye otomatik focus, kapanışta önceki odağa geri dönüş, tekrarlı `aria-label={title}` yerine `useId()` ile `aria-labelledby`. `(app)/layout.tsx`'e skip-to-content linki (`#main-content`) eklendi. `NavLink`'e `aria-current={isActive ? "page" : undefined}` eklendi (aktif menü öğesi artık ekran okuyuculara bildiriliyor). `Sidebar` ve `MobileNavDrawer`'daki `<nav>` öğelerine `aria-label="Ana menü"` eklendi; mobil çekmece paneline `role="dialog" aria-modal="true" aria-label="Ana menü"` eklendi (tam focus trap'i yok, sadece landmark/semantik — ESC ile kapanma zaten vardı). Tüm değişiklikler sonrası `npx tsc --noEmit` / `npm run lint` / `npm run build` temiz geçti, dev server ayakta.
- **20 Temmuz 2026 (performans denetimi):** `src/lib/income/export.ts` (`xlsx`) ve `src/lib/esnaf/sale-receipt-pdf.ts` (`jspdf`) modül tepesindeki statik importlardan `await import(...)` (fonksiyon içi lazy-load) deseniyle değiştirildi — her iki paket de sadece kullanıcı ilgili butona tıkladığında indiriliyor, `/income` ve satış sihirbazının bulunduğu route'ların ilk yükleme JS'inde artık yer kaplamıyorlar. `src/app/(app)/groups/[id]/page.tsx`'teki `getGroup`/`getGroupExpenses` sıralı çağrıları bağımsız oldukları (RLS ikisini de zaten süzüyor) için `Promise.all` ile paralelleştirildi. Tüm `src/lib/data/*` ve `src/app/(app)/**/page.tsx` dosyaları döngü-içinde-await (N+1) ve gereksiz sıralı-await açısından tarandı — geri kalanların hepsi ya zaten `Promise.all` kullanıyor ya da gerçek bir veri bağımlılığı zinciri (örn. Personel & Maaş sayfasında `ensureSalaryRecords`'ın `getEmployees` sonucuna, `getSalaryPayments`'ın da `ensureSalaryRecords`'ın tamamlanmasına ihtiyacı var) — bunlar paralelleştirilemez, doğru şekilde sıralı. Perakende POS'taki `quick_products.sale_count` artırma döngüsü (satır satır read-then-write) bilinçli olarak dokunulmadı — kod içinde zaten "best-effort, tek kullanıcılı POS için yeterli" yorumuyla belgelenmiş bir tasarım kararıydı. "Gereksiz client component" taraması yapıldı (hook/handler kullanmayan ama `"use client"` olan dosyalar arandı) — bulunan 10 dosyanın hepsinin gerekçesi vardı (recharts/tarayıcı-only render gerektiren grafik bileşenleri, Server→Client sınırı ihlalini önlemek için bilinçli client yapılmış `Sidebar`, provider/subscription bileşenleri) — gereksiz hiçbiri yoktu.
- **20 Temmuz 2026 (deployment hazırlığı sırasında bulunan hata):** `.env.example` VE `.env.local`'in her ikisinde de son satırda literal bir `</content>` metni bulundu — daha önceki oturumlarda tespit edilip her büyük `Write` çağrısından sonra `grep -rl "</content>" src/` ile taranan bilinen bir yapay çıktı hatasıyla (agent'ın Write tool çıktısını yanlış parse etmesi) birebir aynı desen. Ancak tarama sadece `src/` altına bakıyordu, kök dizindeki bu iki dosyayı hiç kapsamamıştı. Her ikisi de temizlendi, ardından **tüm repo** (`node_modules`/`.next`/`.git` hariç) yeniden tarandı — başka bir yerde bulunamadı. **Ders:** bundan sonraki `</content>` taramaları `src/` ile sınırlı kalmamalı, kök dizindeki `.env*`/config dosyalarını da kapsamalı.
- **20 Temmuz 2026 (Playwright smoke test):** Oturum açmamış kullanıcı için erişilebilir sayfalar Playwright ile otomatik test edildi: `/dashboard`'a oturumsuz gidince `/login`'e yönlendirme, `/login`/`/register`/`/forgot-password` formlarının doğru alanlarla render olması, `/`'in mantıklı bir yere yönlenmesi, 390px'te yatay taşma olmaması, ziyaret edilen sayfalarda tarayıcı konsol hatası olmaması — 8/8 test geçti. **Sınır:** gerçek kullanıcı kimlik bilgileri olmadığı için Dashboard içeriği, Esnaf modülleri, Realtime senkronizasyon gibi oturum gerektiren akışlar bu otomatik testin kapsamı DIŞINDA — bunlar hâlâ kullanıcının kendi hesabıyla tarayıcıda elle teyit edilmesini bekliyor (bkz. Açık Sorular).
- **20 Temmuz 2026 (Açık Sorular listesi ele alınmaya başlandı):** Kullanıcı "hepsini yap bence" dedi — ~15 maddelik ertelenmiş özellik listesi tek tek ele alınıyor, her biri kendi commit'i olarak. İlk iki madde:
  1. **Abonelikler Dashboard kutusu** — mobildeki `dashboard_screen.dart`'taki `_FixedExpensesSection` (aktif abonelikler + aylık toplam rozeti + yenilenme tarihine göre 3 gün kala turuncu/urgent vurgu) birebir port edildi (`src/components/modules/dashboard/subscriptions-section.tsx`, `src/lib/subscriptions/helpers.ts`). **Bilinçli isim farkı:** mobildeki widget'ın class'ı `_FixedExpensesSection` ve başlığı "SABİT GİDERLER" ama içeriği aslında abonelikler — web'de sayfada zaten gerçek `fixed_expenses` verisini gösteren ayrı bir "Sabit Giderler" kartı olduğu için aynı başlığı kullanmak kafa karıştırırdı, bu yüzden "Abonelikler" olarak etiketlendi (içeriğe sadık, başlığa değil). Urgent renklendirme için `text-warning` yerine (kontrast kuralı — bkz. 18 Temmuz notu) `text-accent` kullanıldı, mobilin turuncusu yerine.
  2. **Esnaf Modu — işletme logosu/adres.** Mobil kaynak ikiye ayrılıyormuş: `business_setup_wizard.dart`'ın Adım 5'i gerçek bir "Adres" (3 satırlı) alanı içeriyor — bu `business-setup-form.tsx`'e eklendi. Logo ise farklı bir yerde, ayrı bir `business_settings_screen.dart`'ta yönetiliyor (mobilde henüz web'de karşılığı olmayan bir "İşletme Ayarları" ekranı) — bu ekran YENİ olarak inşa edildi: `/esnaf/ayarlar` route'u + sub-nav'a "Ayarlar" sekmesi + `BusinessLogoForm` (dosya seç → `business_logos` public bucket'ına `{userId}/{businessId}/logo_{timestamp}.{ext}` yoluna yükle — storage RLS policy'sinin `auth.uid()::text = (storage.foldername(name))[1]` kontrolüyle birebir eşleşiyor — → `businesses.logo_url` güncelle). Bu bucket + kolon `039_business_logo.sql` migration'ıyla zaten production'da mevcut. **Bilinçli olarak taşınmayan:** aynı ekrandaki "Mağaza Verilerini Sıfırla"/"Mağazayı Tamamen Sil" — bkz. Açık Sorular (mobil kaynağın kendisi tutarsız/eksik, güvenilir referans değil, yıkıcı bir özellik güvenilmez kaynaktan kopyalanmadı).
  3. **Harcama formunda özel kategori ekleme.** Mobildeki `CategoryPickerSheet`'in "Yeni" mini-formu (`shared/widgets/category_picker_sheet.dart`) — Kategori Adı + Grup (mevcut kategorilerdeki distinct `parent_group` değerlerinden) + Emoji (opsiyonel) — `src/lib/data/categories.ts`'e `createCategory()` olarak, `src/components/modules/expenses/category-add-dialog.tsx`'e `CategoryAddInline` olarak port edildi. **Bilinçli tasarım farkı:** mobil bunu ayrı bir bottom-sheet olarak açıyor; web'de bu formun zaten kendisi bir `Dialog` içinde olduğu için (`ExpenseFormDialog`), ikinci bir iç içe `Dialog` açmak iki ayrı Escape/Tab focus-trap'inin çakışmasına yol açardı (biri diğerini yanlışlıkla kapatabilirdi) — bunun yerine "Kalemler" başlığının yanına satılan `satis-sihirbaz-dialog.tsx`'teki "Yeni Müşteri Ekle" ile aynı desende bir INLINE aç/kapa mini-form kondu. Eklenen kategori o an açık olan formda anında (`extraCategories` local state, `router.refresh()` beklemeden) tüm kalem satırlarının kategori listesine düşüyor.
  4. **Özet Oluştur (`/reports`) — Excel export.** Mobilin `summary_dashboard_screen.dart`'ı, `/income` sayfasıyla AYNI `ExportService().exportToExcel(incomes, fixed, expenses, periodLabel)` çağrısını kendi dönem verisiyle yapıyor — web'de de yeni bir export fonksiyonu YAZILMADI, zaten var olan `exportBudgetToExcel()` (`src/lib/income/export.ts`, bu oturumda az önce lazy-load'a çevrilmişti) doğrudan yeniden kullanıldı. Bunun için `getReportData()`'ya (`src/lib/data/reports.ts`) iki ek paralel sorgu eklendi (`income_categories`, `fixed_expense_categories` → `incomeCategoryMap`/`fixedCategoryMap`), `ReportView`'a `ExcelExportButton` (`src/components/modules/reports/excel-export-button.tsx`) eklendi, "Yazdır / PDF Kaydet" butonunun yanına.
  5. **Analiz — Genel Kategoriler taksit doğrulaması.** Mobil kaynak (`analytics_service.dart`) bu kez TAM olarak okundu: `getParentCategoryBreakdown` (Genel Kategoriler sekmesi) `getCategoryBreakdown`'un (Kategori Dağılımı sekmesi) aksine **installment-bounded özel durumu HİÇ uygulamıyor** — taksitli bir harcamanın TAM/ham `total`'ını, sadece `date` aralığı filtresiyle (ay bazlı bounding olmadan) diğer harcamalarla aynı şekilde işliyor. Bu, mobil tarafında `getCategoryBreakdown`'a sonradan eklenen düzeltmenin kardeş fonksiyona hiç uygulanmamış olduğu bir TUTARSIZLIK/gözden kaçırma gibi görünüyor (bug, tasarım kararı değil). Web'in paylaşılan `categoryBreakdown()` fonksiyonu (`src/lib/data/analytics.ts`) HER İKİ sekme için de installment-bounding'i zaten tutarlı uyguluyor — bu, mobildekinden DAHA DOĞRU bir davranış. **Karar: kod DEĞİŞTİRİLMEDİ** — Toptancı modülündeki "Cari Hesap" yanlış-yönlendirme bug'ında olduğu gibi, mobildeki bir tutarsızlık bilerek kopyalanmadı; web zaten daha tutarlı/doğru davranıyor.
  6a. **Analiz — Bütçe/Limit kartı.** `analytics_service.dart`'taki `getCategoryLimits()`/`setLimit()`/`deleteLimit()` (`category_limits` tablosu — `001`/`006_limits_overhaul`/`007_card_label` migration'larıyla zaten production'da mevcut, 3 limit türü: kategori/aylık toplam/ödeme yöntemi+opsiyonel kart etiketi, her tür kendi partial unique index'iyle upsert-by-natural-key) `src/lib/data/limits.ts`'e birebir port edildi — harcama+abonelik verisinden "harcanan" hesaplaması, %80'de "warning", harcanan>limit'te "aşıldı" durumu, gelecek aylar için "Planlandı" rozeti (harcanan gösterilmiyor). Analiz → Harcamalar sekmesine `BudgetLimitsCard` + `LimitFormDialog` eklendi. **Bilinçli sadeleştirme:** mobil gelecek 12 ay için limit planlamaya izin veriyor, web 6 ay ile sınırlandı (yeterli, UI daha sade).
  7. **Serbest Meslek & Proje — Pomodoro modu.** `gorevler_screen.dart` satır satır okunarak port edildi: Kronometre/Pomodoro mod seçici (aktif bir kayıt varken kilitli, mobille birebir), 25/50/90dk hazır süreler + "Özel" dakika girişi, geri sayım gösterimi (`hedef − geçen süre`), hedefe ulaşılınca otomatik durdurma + "Pomodoro tamamlandı! X dakika odaklandınız." bildirimi. **Gerçek bir hata bulunup düzeltildi:** ilk yazımda `elapsedSeconds` state'i `useState(activeLog ? Date.now() - ... : 0)` içinde hesaplanıyordu — React Compiler bunu "render sırasında impure fonksiyon çağrısı" (`Date.now()`) olarak reddetti (derleme hatası, `react-hooks/purity`). Çözüm: state `0` ile başlatılıp gerçek hesaplama `useEffect` içine taşındı (mount'ta bir kez + saniyede bir `setInterval` ile), render gövdesinde artık hiçbir impure çağrı yok.
  9. **Serbest Meslek & Proje — AI Masraf Tarayıcı.** Mobildeki `freelance_pano_screen.dart`'taki `_ScanExpenseSheet` (`scan-receipt` fonksiyonuna kategori listesi `{name, group:'Proje Masrafı', emoji:'💰'}` şeklinde gönderiliyor, dönen `items[].category`'yi sabit 5 kategoriyle (Yol/Yemek/Yazılım/Malzeme/Diğer) iki yönlü substring eşleştirmesiyle karşılaştırıyor) `src/lib/esnaf/scan.ts`'e `scanProjectExpense()` olarak port edildi — Evrak Arşivi'nin `scanBusinessDocument()`'ıyla aynı `scan-receipt` fonksiyonunu, farklı kategori eşlemesiyle yeniden kullanıyor (yeni edge function YOK). **Bilinçli mimari fark:** mobildeki tarayıcı business-geneli bir bottom-sheet (tarama sonrası hangi projeye ait olduğunu soruyor); web'de zaten proje bazlı açılan `ProjectExpenseDialog`'a (belirli bir projenin Masraflar sekmesinden) entegre edildi — proje seçimi sormaya gerek kalmadı, dialog zaten bir projeye bağlı. Tarama sonrası tutar/tarih/kategori formu otomatik dolduruyor, kullanıcı onaylayıp kaydediyor (`is_ai_scanned: true`).
  11. **Hizmet & Bakım — sektöre özel Pano (Akıllı Bildirimler).** Mobilde önceki oturumda "usta primleri" olarak not edilen kısmın aslında bu sektöre özgü OLMADIĞI, genel bir "workspace" (çok kullanıcılı personel) özelliği olduğu bu turda netleşti (bkz. Açık Sorular, ayrı ve çok daha büyük bir altyapı kararı). Gerçekten Hizmet & Bakım'a özel olan tek şey `hizmet_pano_screen.dart`'taki "Akıllı Bildirimler" (bakım hatırlatmaları) — bu port edildi: `ServiceJobService.getMaintenanceReminders()` → `src/lib/data/hizmet.ts`'e `getMaintenanceReminders()` (son 210 gün içinde tamamlanmış `service_jobs`, müşteri bazında en son iş, gün farkına göre 3 pencere: 25-42 gün "aylık bakım", 85-100 gün "3 aylık bakım", 160-200 gün "6 aylık bakım" — pencere dışındakiler hatırlatma değil). `/esnaf/pano`'ya `business.business_type === "hizmet"` olduğunda koşullu `SmartRemindersSection` eklendi — yatay kaydırmalı kartlar, her birinde WhatsApp mesaj şablonu (mobille birebir aynı metin) `whatsappLink()` (Hizmet & Bakım modülünde zaten var olan helper) ile gönderiliyor. Bu, `/esnaf/pano`'nun ilk kez GERÇEKTEN sektöre göre farklılaşan içerik göstermesi (şimdiye kadar tüm sektörlerde birebir aynıydı).
  13. **Hizmet & Bakım — usta primleri (staff_commissions).** Bir önceki oturumun bu tabloyu "genel workspace/çoklu-personel özelliğinin parçası, Hizmet'e özgü değil" diye değerlendirmesi YANLIŞTI — `atolye_screen.dart` daha dikkatli okunduğunda, primin aslında Atölye Kanban'ının "Tamamla" aksiyonuna bağlı olduğu görüldü: bir iş "Hazır"dan "Tamamlandı"ya taşınırken `_showCompletionDialog` açılıyor — (varsa) açık hesap ödemesini nakit/karta çeviriyor, işi yapan personeli atıyor, ve personel seçilirse bir prim oranı (%) alıp `staff_commissions`'a (zaten var olan `employees` tablosunu kullanıyor, YENİ bir "workspace" kavramı değil) kaydediyor. **Gerçek bir boşluk bulundu:** `staff_commissions` tablosunu oluşturan HİÇBİR migration mobil kaynak reposunda yok (muhtemelen Dashboard'dan elle eklenmiş) — web tarafı `docs/sql/048_staff_commissions.sql`'i (kolonlar mobil kaynağın insert/select çağrılarından çıkarıldı: business_id/user_id/staff_id/job_id/amount/commission_rate/period_month) `CREATE TABLE IF NOT EXISTS` + tam GRANT+RLS + Realtime kaydıyla YENİ yazdı — kullanıcının bunu SQL Editor'da çalıştırması gerekiyor. Web tarafı: `src/lib/data/hizmet.ts`'e `completeServiceJob()` (mobildeki `_showCompletionDialog`'un onay mantığıyla birebir) + `getStaffCommissionSummary()`, yeni `ServiceJobCompleteDialog` bileşeni (Atölye'nin "Tamamla" butonunu artık doğrudan durum değiştirmek yerine bu dialogu açacak şekilde değiştirdi), Personel & Maaş sayfasına "Bu Ay Primler" özet kartı eklendi.
  15. **Hızlı Perakende — Satış Geçmişi.** `perakende_satis_gecmisi_screen.dart` port edildi: `perakende_transactions` + `perakende_transaction_items` join'i, dönem filtresi (Tümü/Bugün/Bu Hafta/Bu Ay — mobildeki "Özel tarih aralığı" bu turda dahil edilmedi, 3 hazır filtre yeterli görüldü), satıra tıklayınca kalem detaylarını açan genişleyebilir kartlar, ödeme yöntemi ikonu + "AI tarama" rozeti. Web'in `getRecentTransactions()` fonksiyonu zaten VARDI ama hiçbir yerde kullanılmıyordu (muhtemelen POS inşa edilirken ileride kullanılmak üzere önceden yazılmış) — items join'i ve tarih filtresi eklenerek genişletildi, yeni `/esnaf/perakende/gecmis` route'u + sub-nav'a "Geçmiş" sekmesi eklendi. **Not:** "Perakende Giderleri" ve "Ayarlar" ekranlarının web'de zaten var olan işlevlerin (Kasa Defteri, kalıcı sekme çubuğu) tekrarı olduğu bu incelemede netleşti — taşınmadı, bkz. Açık Sorular. **Önemli altyapı notu:** `fisleapp` reposunda İKİ AYRI migrations klasörü olduğu keşfedildi — `fisle_app/supabase/migrations/` (53 dosya, 001-050+, aktif/güncel) ve kök dizindeki `supabase/migrations/` (15 dosya, eski/muhtemelen terk edilmiş bir öncül, ama `029_perakende_modulu.sql` ve `030_hizmet_modulu.sql` SADECE bu eski klasörde bulunuyor — `fisle_app` klasörü numaralamada 028'den 031'e atlıyor). Perakende/Hizmet modülleri için şema araştırması bu yüzden kök dizindeki klasörden yapıldı; ileride bu iki sektörle ilgili migration aranırken bu ayrım akılda tutulmalı.
  17. **Hızlı Perakende — PLU kod arama + ürün varyasyonları.** Şema baştan hazırdı (`quick_products.product_code`/`has_variations`, `product_variations` tablosu — orijinal `029_perakende_modulu.sql`'de GRANT+RLS'iyle birlikte zaten vardı, sadece UI hiç kullanmıyordu). `add_quick_product_screen.dart` + `hizli_kasa_screen.dart` port edildi: Ürün formuna PLU kodu alanı + "Varyasyonlu ürün" toggle'ı + varyasyon satırları (etiket+fiyat) eklendi. **Bilinçli iyileştirme:** mobil varyasyonları SADECE ürün oluştururken düzenlemeye izin veriyordu (düzenleme ekranı hiç dokunmuyordu — kod içinde bunun farkında olunduğunu gösteren bir yorum bile vardı: *"Wait, svc.create doesn't take imageUrl parameter... I can't be sure"*) — web'de bu tam bir CRUD'a genişletildi (aynı tablo/kolonlarla, yeni davranış icat edilmedi, sadece eksik olan düzenleme senkronizasyonu eklendi). Kasa (POS) tarafında: PLU arama kutusu (yazarken listeyi filtreliyor, 2+ haneli tam eşleşmede otomatik sepete ekliyor — `useEffect` içinde `searchProductByCode()` çağrısı), varyasyonlu bir ürüne tıklayınca varyasyon seçim dialogu (+ "Varyasyonsuz ekle" seçeneği), sepet satırları artık `variationLabel` taşıyor ve `perakende_transaction_items.variation_label`'a yazılıyor (kolon zaten vardı, web'in tipinde de zaten tanımlıydı).
  19. **Hızlı Perakende — gün sonu toplu fiş tarama.** `gece_scan_screen.dart` port edildi: `PerakendeGunSonuDialog` (Evrak Arşivi'ndeki `DropzoneUploader` + per-file tarama döngüsü deseniyle) — her fiş görseli `scan-receipt`'e boş kategori listesiyle gönderiliyor (sadece toplam tutar lazım, kalem/kategori eşlemesi gerekmiyor), okunan tutar düzenlenebilir bir alanda gösteriliyor, "Tümünü Kaydet" her fiş için ayrı bir sepetsiz `perakende_transactions` kaydı (`is_ai_scanned: true` + `image_url`) oluşturuyor. **Şema notu:** `perakende_transactions.image_url` kolonu orijinal migration'da YOKTU — ayrı, doğru şekilde izlenen bir migration'da (`fisle_app/supabase/migrations/045_perakende_image_url.sql`) sonradan eklenmiş, bu web'de zaten üzerinde durulmasına gerek kalmadan production'da mevcuttu (staff_commissions'daki izlenmeyen-migration durumundan farklı olarak burada güvenle varsayılabildi).
  20. **Hedeflerim — `savings_pool` (birikim havuzu) akışı.** `goals_screen.dart` satır satır okunarak gerçek akış çıkarıldı: web'deki "Para Ekle"nin doğrudan hedefe eklediği eski davranış YANLIŞTI (mobilde asla öyle çalışmıyor) — gerçek akış iki adımlı: (1) net bakiyeden **havuza** aktarım (`transferToPool`, basit read-then-write + `goal_transactions` kaydı, mobille birebir), (2) havuzdan **belirli bir hedefe** aktarım — bu adım mobilde zaten atomik bir `SECURITY DEFINER` RPC'ye (`allocate_to_goal`, `039_security_hardening.sql`'de tanımlı, GRANT authenticated'a verilmiş, production'da zaten mevcut) taşınmıştı (yorum: "Önceki okuma-güncelleme döngüsü eşzamanlı çağrılarda yarış koşulu yaratıyordu") — web de YENİ SQL yazmadan doğrudan bu RPC'yi çağırıyor (`supabase.rpc('allocate_to_goal', ...)`), havuz bakiyesi ve hedef limiti sunucu tarafında doğrulanıyor. Hedef silinince biriken tutar havuza iade ediliyor (`returnSavedAmountToPool`, mobille aynı). Yeni bileşenler: `SavingsPoolCard` (gradient hero kart, `/goals` sayfasının en üstünde), `AddToPoolDialog` (havuza ekleme), `ContributeDialog` yeniden yazıldı → artık havuzdan-hedefe aktarım (Max butonu, "Havuz bakiyesi yetersiz"/"Hedef tutarını aşamazsınız" doğrulamaları, havuz boşsa "+ Ekle" kısayolu). `savings_pool` Realtime tablo listesine (`(app)/layout.tsx` + `047_web_realtime.sql`) eklendi.
  21. **Gruplarım — davet/katılma akışı + üye listesi + sohbet.** Bu, oturumun en büyük tek parçası (mobil kaynakta ~5474 satır) — önce bir Explore ajanıyla TÜM özellik (şema, RPC'ler, davet/katılma, üye listesi, sohbet, harcama paylaştırma, takma adlar) baştan sona araştırıldı. **Kritik meta-bulgu:** `fisleapp` reposundaki iki migrations klasörü (bkz. 19 Temmuz notu) birbirinin KOPYASI değil, ikisi de FARKLI zamanlarda canlıya uygulanmış GERÇEK dallar — kanıt: `fisle_app`'in kendi `022`/`039` migration'ları, sadece eski üst-dizin klasöründeki `017_groups_social.sql`'in oluşturduğu kolonlara (`group_members.left_at`, `group_invites.created_by`) referans veriyor. Bu yüzden "hangi klasör güncel" sorusu yanlış soru — ikisinin BİRLEŞİMİ gerçek şemayı oluşturuyor.
      - **Davet/katılma:** `accept_group_invite(p_token text) RETURNS boolean` RPC'si (`fisle_app/048_fix_admin_and_group_join.sql`, `SECURITY DEFINER`, prim tier'ına göre üye limiti — free owner=2, premium/trial owner=6) zaten production'da mevcut, RLS'i tamamen atlıyor — web bunu doğrudan çağırıyor, yeni SQL gerekmedi. **Ama davet ÖNİZLEMESİ (`getInviteByToken`) farklı bir konu:** mobil bunu düz bir client-side SELECT ile okuyor ama `group_invites` üzerindeki RLS politikalarının davet EDİLEN (sahip olmayan) bir kullanıcının bu satırı görmesine izin verip vermediği iki çelişkili migration dalı arasında canlı veritabanı sorgulanmadan kesin olarak doğrulanamadı. Riske girmek yerine `accept_group_invite` ile AYNI SECURITY DEFINER desende YENİ bir `get_invite_preview(p_token text)` RPC'si yazıldı (`docs/sql/049_group_invite_preview.sql`, kullanıcının çalıştırması gerekiyor). Web akışı: Gruplarım listesinde "Davet Kodu Gir" (mobildeki `_parseToken` regex'i birebir — kod veya `.../join/<token>` linki kabul ediyor) → `/join/[token]` sayfası (RPC ile önizleme + "Katıl"/"Reddet") → grup sahibi için grup detayında "Davet Et" → `InviteDialog` (yeni token oluşturur, önceki pending daveti expire eder, linki kopyalar, 7 gün geçerli — DB default'u).
      - **Üye listesi:** `getMembers()` — aktif üyeler (`left_at IS NULL`) + `user_aliases` overlay'i (her kullanıcının başkalarına verdiği ÖZEL takma ad, mobille birebir: `customAlias ?? name ?? email`). Sahip: üye başına yönetici yap/üye yap + çıkar (soft-delete `left_at`) menüsü. Herkes: kendine özel takma ad belirleyebiliyor. **Bilinçli olarak REPLİKE edilen bir mobil sınırlama:** sahip grubu asla "sahiplik devri" yapmadan bırakamıyor, sadece grubu tamamen silebiliyor (mobilde de öyle, icat edilmedi) — `GroupDangerZone` bunu birebir yansıtıyor (sahip: "Grubu Sil", üye: "Gruptan Ayrıl").
      - **Sohbet:** `group_messages` tablosu (zaten mobilin kendi Realtime publication kurulumunda vardı, yeni SQL gerekmedi). `GroupChatCard` — mesaj gönder/sil (kendi mesajın)/sohbeti temizle (sadece sahip — mobildeki UI kısıtı `_isGroupOwner` ile birebir, RLS aslında admin'e de izin veriyor ama mobil UI'ı göstermiyor, bu tutarsızlık da birebir kopyalandı). **Önemli mobil-doğru detay:** gönderen adı, realtime payload'ından DEĞİL, önceden çekilmiş üye listesinden (`nameMap`) çözülüyor — Supabase Realtime satırları `users()` join'i taşımıyor, mobil de aynı workaround'u kullanıyor. Realtime için sayfa-özel `useRealtimeRefresh(["group_messages"])` çağrısı kullanıldı (global layout listesine eklenmedi, sadece grup detay sayfasında mount'lanıyor).
      - **Bilinçli olarak henüz TAŞINMAYAN, gerçek bir mobil hata bulunduğu için:** `GroupSplitService.getSettlementSummary()` ("kim kime borçlu" hesaplaması) mobilde `expenses.group_id` (asla doldurulmayan, terk edilmiş bir legacy kolon — gerçek expense↔group bağlantısı sadece `expense_groups` junction tablosu üzerinden) üzerinden sorgu yapıyor, bu yüzden PRODUCTION'DA HER ZAMAN BOŞ SONUÇ DÖNÜYOR — hiçbir ekranın da bu metodu çağırdığı bulunamadı (muhtemelen hiç tamamlanmamış/ulaşılamayan kod). Bu bozuk sorguyu birebir kopyalamak yerine, harcama paylaştırma (eşit bölme + "Ödedim" kendi-beyanı işaretleme — bunlar ÇALIŞIYOR) + yorum/reaksiyon bir SONRAKİ iş olarak bırakıldı; eğer/ne zaman inşa edilirse settlement hesaplaması `expense_groups` üzerinden DOĞRU şekilde yeniden yazılacak, mobildeki bozuk sorgu asla kopyalanmayacak.
      - **`useEffect` içinde senkron `setState` hatası (tekrarlayan desen):** `InviteDialog` ilk yazımda `useEffect` içinde `setLoading(true)`'ı senkron çağırıyordu — React Compiler yine reddetti (`react-hooks/set-state-in-effect`, bu oturumda üçüncü kez karşılaşılan desen). Çözüm yine aynı: `loading` state'i `useState(true)` ile başlatıldı (senkron reset'e gerek kalmadı), çağıran taraf (`MemberList`) `key={inviteOpen ? "open" : "closed"}` ile her açılışta bileşeni yeniden mount ediyor.
  22. **Gruplarım — harcama paylaştırma (split) + yorum/reaksiyon.** `group_split_service.dart`/`group_comment_service.dart`/`group_expense_detail_screen.dart` port edildi: yeni `/groups/[id]/expenses/[expenseId]` sayfası — 4 sabit reaksiyon (`like`👍/`surprised`😮/`laugh`😂/`love`❤️, `expense_reactions` CHECK kısıtına birebir uyuyor, tek kullanıcı-tek reaksiyon, aynısına tekrar basınca kaldırıyor), "Eşit Böl" (mobildeki TEK split modu — şema percentage/fixed'i destekliyor ama hiçbir UI hiç oluşturmuyor, web de sadece eşit bölmeyi sundu), her üye kendi payını "Ödedim" ile kendi-beyanı işaretliyor (gerçek bir ödeme/transfer değil, mobille birebir), yorumlar (max 1000 karakter, sadece kendi yorumunu silebiliyor). **Bilinçli olarak port EDİLMEYEN, gerçek bir mobil hata:** `getSettlementSummary()` — bkz. Kararlar Günlüğü'ndeki 21 numaralı madde, `expenses.group_id` (asla doldurulmayan terk edilmiş kolon) üzerinden sorgu yaptığı için production'da her zaman boş sonuç dönüyor ve hiçbir ekran zaten çağırmıyor; bozuk sorgu kopyalanmadı, ileride gerçekten inşa edilirse `expense_groups` üzerinden doğru yazılacak.
  23. **Menü Yönetimi — tam restoran POS'u (Salon/Adisyon/Sipariş/Paket Servis/Restoran Kasa).** Esnaf Modu'ndaki son büyük açık iş, önce bir Explore ajanıyla mobil kaynak (~3544 satır: `kafe_salon_screen.dart`, `masa_adisyon_screen.dart`, `siparis_ekle_screen.dart`, `paket_servis_screen.dart`, `kafe_kasa_screen.dart`) baştan sona araştırıldı. Şema (`031_kafe_restoran.sql` + `034_menu_extras.sql` + `035_guest_count.sql`, hepsi `fisle_app` klasöründe — bu özellik için iki-migrations-klasörü ayrımı geçerli değildi, hepsi tek yerde) zaten GRANT+RLS'iyle production'da mevcuttu, yeni SQL gerekmedi. **DB seviyesinde trigger YOK** — durum geçişleri (masa boş/dolu/hesap istendi) ve `order_items`→`restaurant_orders.total_amount` yeniden hesaplaması tamamen mobil uygulama kodunda yapılıyormuş, web de aynı client-orkestrasyonu (`src/lib/data/restaurant.ts`) uyguluyor.
      - **Salon:** `KafeSalon` — grid + bölüm (section) sekmeleri (mobil de aslında sürükle-bırak bir kat planı DEĞİL — `position_x`/`position_y` kolonları var ama hiçbir ekran hiç okumuyor/yazmıyor, hep 0 — bu "hayalet" davranış web'de icat edilmedi). 3 durum: boş/dolu/hesap istendi. Masa ekle/düzenle/sil.
      - **Adisyon + Sipariş Ekle:** `KafeMasaAdisyon` + `ItemPickerDialog` (paylaşılan bileşen — hem yeni sipariş açma hem var olan adisyona ekleme hem Paket Servis'ten yeni sipariş için kullanılıyor). Kategori sekmeleri + arama + sepet. **Bilinçli olarak dahil edilmeyen:** menü ekstraları (`menu_item_extras` — şema hazır ama hiçbir ürün formunda ekstra TANIMLAMA UI'ı yok, bu yüzden sipariş alırken de seçilecek bir şey olmazdı) ve "Top Satanlar" filtresi (mobilde `getTopSellerIds()` var olmayan bir `restaurant_order_items` tablosunu sorguluyor — **gerçek bir mobil hata**, `try/catch` içinde sessizce yutuluyor, özellik mobilde de hep boş liste veriyor; web'de bu YANLIŞ sorgu kopyalanmadı, bunun yerine gerçek `order_items` tablosunu doğru grupla­yan `getTopSellingMenuItemIds()` yazıldı ama şu an hiçbir UI'dan çağrılmıyor — ileride "Top Satanlar" filtresi eklenmek istenirse doğru fonksiyon zaten hazır).
      - **Ödeme (Tahsil Et):** `PaymentDialog` — Nakit/Kart/Karışık/"Kişi Başı Hesap" (mobildeki "alman" modu — kişi başı hesap makinesi GÖRÜNÜYOR ama gerçekte tüm tutarı tek seferde nakit tahsil ediyor, mobille birebir aynı — yanıltıcı ama gerçek davranış, icat edilmedi). **Bilinçli olarak port edilmeyen, ulaşılamayan mobil kod:** item-bazlı kısmi ödeme ("parçalı" modu) — kod tam yazılmış ama mod seçim ekranında hiç bir buton bu modu seçmiyor, web de sunmuyor. **Bilinçli DÜZELTME:** mobil, karışık (nakit+kart) bir ödemeyi generic `business_incomes` tablosuna TEK satır ve HER ZAMAN `'cash'` olarak yazıyor (gerçek bir veri-doğruluğu hatası — kart kısmı kayboluyor) — web bunun yerine iki ayrı satır yazıyor (biri nakit, biri kart), aynı insert deseniyle, yeni davranış icat edilmeden sadece doğru yazılıyor.
      - **Paket Servis:** `KafePaketServis` — aynı `restaurant_orders` tablosu (`table_id: null`, `order_type: 'takeaway'|'delivery'`), mobil de bunun ötesinde ayrı bir hazırlanıyor/yolda/teslim edildi durum makinesi TUTMUYOR (sadece "açık" (listede) / "kapandı" (ödenince listeden kayboluyor)) — web de icat etmedi. Adres/telefon alanı da mobilde yok (tek bir serbest metin `customer_name` alanı) — web de aynı.
      - **Restoran Kasa:** `KafeKasa` — generic Kasa Defteri'nden AYRI, kapanmış `restaurant_orders`'ın günlük ciro + masa/sipariş bazlı kırılımı. **Not:** mobil de aynı ödeme hem `restaurant_payments`'a hem (yukarıdaki düzeltmeyle) `business_incomes`'a yazıyor — yani generic Kasa Defteri açılıp kafe işletmesi seçilirse TEORİDE aynı ciro iki ayrı yerden (birbirinden bağımsız hesaplanan ama normalde uyuşması gereken) görünecek; bu, mobildeki gerçek mimari, web'de tek bir "doğru" kaynağa konsolide edilmedi (icat etmemek için).
      - Sub-nav'daki kafe'ye özel splice mantığı (`businessType === "kafe" ? ... : ...`) kaldırılıp diğer 5 sektörle AYNI `SECTOR_TABS` desenine taşındı — artık kafe de her sektör gibi tek bir kayıttan tanımlanıyor, özel durum kodu azaldı.

**Çözülen (referans için tutuluyor):** Supabase projesinde e-posta doğrulamasının açık olup olmadığı — canlı testte `signUp()` çağrısının e-posta gönderme rate-limitine takılması bunun AÇIK olduğunu doğruladı.

- **21 Temmuz 2026 (kullanıcı canlı testte 1 hata + 4 istek bildirdi, tek tek ele alınıyor):**
  1. **Harcamalarım — gün başlıklarına tarih eklendi.** `dateGroupLabel()` (`src/lib/utils/date.ts`) — 2-6 gün önceki kayıtlarda artık sadece "CUMARTESİ" değil "CUMARTESİ · 18 Tem" gösteriliyor. Mobilin `format_utils.dart`'ındaki `dateGroup()` bunu YAPMIYOR (doğrulandı) — bu bilinçli bir web-özel UX iyileştirmesi, kaçırılmış bir mobil özelliğin portu değil.
  2. **Yatırımlarım — canlı fiyat bağlandı.** Önceki oturumda "yeni edge function + API key gerekiyor, bloklu" denmişti — bu YANLIŞTI, araştırılınca `get-investment-prices` adında keyless bir edge function'ın zaten var ve deploy edilmiş olduğu görüldü (Truncgil altın/döviz + CoinGecko kripto, ikisi de ücretsiz/public API, paralel çağrılıyor). `src/lib/data/investments.ts`'e `getInvestmentPrices()` eklendi, `investments-list.tsx` mobildeki `Investment.currentValue`/`profitLoss` getter'larını portlayarak portföy toplamı + satır bazlı kâr/zarar gösteriyor (fiyat çekilemezse eskisi gibi sadece alış maliyetine dönüyor, sessizce kırılmıyor).
  3. **Gruplarım — grafikli analiz sekmesi eklendi (mobilde de YOK, kullanıcının kendi ifadesiyle "bu özellik mobilde de yok").** Kullanıcı hem web'in mobildeki grup detayına (üye/sohbet/paylaşım — bunlar zaten bir önceki turda tamamlanmıştı) parite açısından hem de HİÇBİR platformda olmayan yeni bir "grup bütçesi için grafiksel analiz" özelliği istedi. Kişisel Analiz sayfasındaki kategori/genel-kategori/market kırılım mantığı (`src/lib/data/analytics.ts`'ten `CHART_COLORS`/`parentGroupIcon`/`normalizeStoreName` export edilerek) `expense_groups` join'i üzerinden gruba scope edilip yeniden kullanıldı — yeni bir hesaplama modeli icat edilmedi, var olanın kanıtlanmış pro-rata taksit/kalem mantığı birebir tekrarlandı. Yeni `GroupAnalyticsCard` (`src/components/modules/groups/group-analytics-card.tsx`) — proje kuralı gereği ("bir web sitesi geliştiriyoruz mobil uygulama değil, kaydırarak geçmeli şeyler olmamalı") tıklanabilir `Tabs` kullanıyor, carousel/swipe DEĞİL. `PeriodSelector` ile aynı `period` URL search param deseni (`/groups/[id]` sayfası artık `searchParams` alıyor).
  4. **Ayarlar — Hesap bölümü eklendi.** `AccountDangerZone` (`src/components/modules/settings/account-danger-zone.tsx`): Çıkış Yap, Verileri Sıfırla (mobildeki `UserProfileService.resetAllUserData()`'nın 16 tablosu birebir — sadece kişisel finans verisi, esnaf/grup verisi dokunulmuyor — çift `window.confirm` ile), Hesabı Sil (yeni bir RPC YAZILMADI, `024_medium_security_fixes.sql`'deki mevcut `delete_current_user` SECURITY DEFINER RPC'si çağrılıyor). Ayrıca sayfanın üstüne `/esnaf`'a giden bir "Esnaf Modu" kartı eklendi (zaten var olan route, sadece Ayarlar'dan da erişilebilir hale geldi). **Premium'a Geç / Esnaf Aboneliğine Geç / Bir İşyerine Katıl bu turda YAPILMADI** — bkz. Açık Sorular (ikisi de ayrı, büyük kararlar gerektiriyor: ödeme altyapısı ve workspace/çoklu-personel sistemi).
  5. **"Üye değilim" bug'ı — kök neden bulundu ve düzeltildi.** RLS/politika incelemesi (`004_fix_rls_recursion.sql`, `026_fix_group_members_recursion.sql`) hiçbir sorun göstermiyordu çünkü sorun RLS'de değildi. Kullanıcıya sorulunca hatanın Dashboard'daki "Grup Bütçesi" kutusunda olduğu netleşti (Gruplarım sayfasının kendisi zaten doğru çalışıyordu). Kök neden: `getGroupMembershipCount()` (`src/lib/data/dashboard.ts`) **`family_memberships` diye bir tablodan** okuyordu — bu, Faz 1'de (Gruplarım henüz inşa edilmeden ÖNCE) yazılmış, hiç var olmayan/kullanılmayan bir placeholder tablo referansıydı (repo genelinde grep'lendi, `dashboard.ts` dışında hiçbir yerde geçmiyor). Gerçek Gruplarım özelliği daha sonra tamamen farklı tablolarla (`groups`+`group_members`) inşa edildi ama bu Dashboard sorgusu hiç güncellenmemişti — bu yüzden kullanıcı gerçekten üye olsa bile sayım her zaman 0 dönüyordu. Düzeltme: sorgu `group_members` tablosuna (`user_id` + `left_at IS NULL`, `getMembers()`'ın kullandığı AYNI şema) yönlendirildi. Ayrıca kutunun metni de düzeltildi — "Gruplarım modülünde geliyor" (Faz 1'den kalma, artık yanlış — modül aylardır tamam) yerine gerçek üyelik sayısı + `/groups`'a giden bir link.
  6. **Menü Analizi (yeni, mobilde de YOK).** Kullanıcı bir ekran görüntüsüyle ("Kasa Defteri"nde masa tahsilatlarını gösterdi) iki şey istedi: masaların ne sipariş ettiğini görebilme/tıklanabilirlik + "hangi menüden hangi tarihte ne kadar satıldı" grafiği. Mobil kaynakta (`fisle_app/lib/features/esnaf/screens/`) hiçbir kafe/restoran ekranının `fl_chart` import ETMEDİĞİ doğrulandı (sadece 2 genel rapor ekranında var, o ikisi zaten web'e taşınmıştı) — yani bu özellik gerçekten hiçbir platformda yoktu. **Tıklanabilirlik kısmı zaten VARDI** — Restoran Kasa (`kafe-kasa.tsx`) kapanan her masa/sipariş satırına tıklayınca kalemleri açıyor, Salon'da dolu bir masaya tıklamak zaten Adisyon'a (canlı sipariş kalemleri) götürüyor; kullanıcı muhtemelen bunu görmemişti çünkü baktığı sekme genel Kasa Defteri'ydi (kalem detayı olmayan, sadece tahsilat tutarı gösteren generic ledger — bilinçli olarak ayrı tutulan bir görünüm, bkz. 19 Temmuz notu). **Yeni olan grafik kısmı** eklendi: `getMenuSalesBreakdown()` (`src/lib/data/restaurant.ts`) — `order_items` (kendi `business_id`/`created_at` alanları sayesinde join gerektirmeden) dönem + ürün adına göre gruplanıyor, hem toplam hem gün-gün kırılım dönüyor. `KafeMenuAnalytics` (`src/components/modules/esnaf/kafe-menu-analytics.tsx`) Restoran Kasa sayfasına eklendi — Dönem Seçin (`menuPeriod` ayrı search param, günlük ciro'nun `date` param'ıyla çakışmasın diye) + ciro bazlı bar chart + tıklayınca o ürünün tarih-tarih satış dökümünü açan liste.
  7. **Mobil uygulamaya da bu özelliği ekleme yetkisi verildi ("şimdilik").** Bu, oturum boyunca kesin ve defalarca tekrarlanan "`fisleapp` SADECE referans, asla değiştirilmez" kuralının bir istisnası — kullanıcıdan bu konuda ayrıca teyit istendi (Flutter tarafında bu oturumda hiç kurulmuş bir build/test doğrulama akışı yok), henüz mobil koduna dokunulmadı.
